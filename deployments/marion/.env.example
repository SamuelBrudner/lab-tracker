# Copy this file to .env and replace the placeholder with the existing Marion
# database password. Use a URL-safe value (for example, `openssl rand -hex 32`)
# for a new database. Never rotate this value as part of a routine app release.
MARION_POSTGRES_PASSWORD=replace-with-existing-marion-database-password
