# Copy this file to .env, provide the credential for the one explicitly
# configured provider, and run:
#   chmod 600 deployments/dedicated-instance/provider/.env
DEDICATED_OPENAI_API_KEY=replace-with-provider-api-key
