# Lab Tracker — Re-Review After P1 Fixes

Date: 2026-06-12 · Scope: diff f71ea24..HEAD (commits faceb89, e9342b9, c382cfe).
Method: 12 adversarial fix-verifiers (one per closed P1 bead), 5 reviewers over new/changed code,
2 open-bead recheckers, 3-lens adversarial verification of every medium+ finding, completeness critic
with gap round. 62 agents. Previous full review: docs/runs/deep-review-2026-06-12.md.

## Quality gates

- **pytest:** PASS — 338 passed, 1 warning in 185.61s (0:03:05). Exit code 0. Only warning: StarletteDeprecationWarning from .venv\Lib\site-packages\fastapi\testclient.py (third-party, not project code).
- **ruff:** PASS — `.venv/Scripts/ruff.exe check .` output: "All checks passed!"
- **frontend:** PASS — npm run test:frontend (vitest): 10 test files passed, 62 tests passed, duration 7.95s. npm run lint:frontend (eslint on src/lab_tracker/frontend_src/**/*.{js,jsx}): no output, exit code 0 (clean).
- **summary:** All quality gates pass for C:/Users/snb6/Documents/GitHub/lab-tracker. pytest: 338 passed (1 third-party deprecation warning). ruff: clean. Frontend: 62/62 vitest tests pass and eslint clean. alembic heads prints exactly one head: "0025_dataset_external_artifacts (head)", satisfying the single-head requirement. No failures observed; nothing was modified.

## P1 fix verdicts

### lab-tracker-cq5.1: FIXED (high confidence)

The core defect is fully resolved on every production engine path. src/lab_tracker/db.py:30-31 (added in c382cfe, unchanged since per git log f71ea24..HEAD) calls _configure_sqlite_engine for any database_url starting with 'sqlite', and the listener (db.py:36-44) executes PRAGMA foreign_keys=ON, journal_mode=WAL, busy_timeout=5000 on every DBAPI connect. Path audit: (1) app runtime — src/lab_tracker/app_parts/runtime.py:45 'engine = get_engine(settings)'; (2) get_session_factory — db.py:53 'bind=engine or get_engine(settings)' uses get_engine when no engine is injected, and the only production injector is runtime.py:46 which passes the get_engine-built engine; (3) dolt mirror — src/lab_tracker/dolt_mirror.py:81 uses get_engine(); (4) module-level SessionLocal — db.py:61 via get_session_factory(). The required regression test exists: tests/test_db_wiring.py:101-138 asserts PRAGMA foreign_keys==1, busy_timeout==5000, journal_mode=='wal' (lines 112-114) and that deleting a ProjectModel cascades away its QuestionModel (lines 130-138). I ran tests/test_db_wiring.py: 7 passed. Adversarial probes I executed against the live code: pragmas persist on pooled connection reuse (fk=1, busy_timeout=5000 on second checkout); inserting a QuestionModel with a dangling project_id raises IntegrityError; 'sqlite:///:memory:' also gets fk=1 (journal_mode stays 'memory', a harmless no-op); and a raw 'DELETE FROM projects' with FK ON correctly cascades through datasets/sessions/questions despite datasets.primary_question_id (db_models.py:146) and sessions.primary_question_id (db_models.py:445) being NO ACTION FKs — SQLite end-of-statement FK checking handles the multi-path cascade, so no new 500 path on project deletion (also confirmed by tests/test_http_db_integration.py:250+ deleting a project that still holds a committed dataset, status 200, under the FK-on conftest client fixture). Remaining uncovered paths from the verification list: alembic/env.py:44 builds its engine via engine_from_config with no pragmas — I judge this acceptable, not a defect: no migration performs data deletes (grep over alembic/versions shows only UPDATE statements in 0008/0023), FK-off is the recommended state for SQLite batch table rebuilds, pysqlite's default connect timeout already provides a 5s busy wait, and WAL is a persistent per-file property set on first app connect. Test engines are the one real gap (reported as the single low-severity newIssue): a dozen test files create raw engines without the pragmas, so service/repository-level tests run with FK enforcement off, unlike production; the HTTP integration suite does cover the FK-on path, which keeps the defect itself fixed. Known-issue check: open beads under epic lab-tracker-cq5 (cq5.5-cq5.10) track delete-guard, goal-link, membership, and migration-drift follow-ups; none tracks the test-engine pragma parity gap, and the cq5.7/NOT-NULL-drift finding in the report is a different divergence (schema nullability, not pragmas).

**Test coverage:** tests/test_db_wiring.py::test_sqlite_engine_enforces_foreign_keys_and_busy_wal_pragmas (lines 101-138) directly asserts all three pragmas on a get_engine-built engine and that a project delete cascades to its question rows; ran locally, 7/7 tests in the file pass. Indirect coverage: the entire tests/test_http_db_integration.py suite runs against an app built by create_app -> runtime.build_app_runtime -> get_engine (tests/conftest.py:47-55), so its CRUD/delete flows execute with FK enforcement on, including project deletion with live dataset/session rows holding NO ACTION question FKs.

**New issue found during verification** [low/testing] `tests/api_helpers.py:17` — Service/repository test engines bypass SQLite FK pragmas, so most of the suite still runs with FK enforcement off

The fix registers the pragma listener only inside get_engine (src/lab_tracker/db.py:30-31), but the bespoke test engines never go through it: tests/api_helpers.py:17, tests/test_sqlalchemy_repository.py:45, tests/test_persistence_flows.py:98/160/202/240/334/580/692/735/802, tests/test_service_composition.py:46, tests/test_observability.py, tests/test_device_auth.py:27, tests/test_auth_routes.py:22 all call create_engine directly with no foreign_keys pragma (grep for 'foreign_keys' in tests/ hits only test_db_wiring.py). tests/test_persistence_flows.py:98-104 even passes its raw engine into get_session_factory(engine=engine), which silently skips pragma configuration because db.py:52-53 only applies pragmas when it builds the engine itself. Consequence: the service- and repository-level tests — exactly the layer where the delete-path/FK-guard logic lives — exercise a SQLite that does not enforce FKs, diverging from production. A repository bug that writes a dangling FK or deletes a guarded parent passes these tests and surfaces in production as an IntegrityError 500. Mitigation that keeps this low severity: the HTTP integration suite (tests/conftest.py client fixture -> create_app -> runtime.py:45 get_engine) does run full CRUD flows with FK enforcement on. The deep-review fix suggestion explicitly offered the global form listens_for(Engine, 'connect') keyed on dialect, which would have covered all engines; the narrower per-engine form was chosen instead. Not tracked by any open bead (cq5.7 covers the related but distinct create_all-vs-migrations nullability drift).

Fix: Either register the pragmas globally for the sqlite dialect (event.listens_for(Engine, 'connect') with a dialect check, as the review report suggested), or extract a public configure_sqlite_engine helper and call it from a shared test fixture so service-level tests run with FK enforcement matching production.

### lab-tracker-cq5.2: PARTIAL (high confidence)

The fix in c382cfe adds QuestionService._ensure_question_not_primary_reference (src/lab_tracker/services/question_service.py:386-408), called from delete_question (line 381). It correctly guards the two FK paths that have no ondelete policy: datasets.primary_question_id (src/lab_tracker/db_models.py:144-148, mirrored in alembic/versions/0002_core_entities.py:55) and sessions.primary_question_id (db_models.py:443-446, alembic 0002:81). The guard's same-project scan is sound because cross-project primary references are rejected at every creation/update path (dataset_service.py:96-97 'Primary question must belong to the same project', 193-194 for question_links updates; session_service.py:95-96 and 253-254 for create/promote). query_from_repository correctly unwraps the (list, total) tuple (services/base.py:97-103). ValidationError maps to HTTP 422 (routes/errors.py:24-30), so no 500. The test test_delete_question_rejects_primary_dataset_and_session_references (tests/test_core_api.py:691-726) covers both guarded paths and passes (ran: 2 passed).

However, the third path named in the original finding (H11 in docs/runs/deep-review-2026-06-12.md:124-130: 'dataset_question_links.question_id IS ondelete=CASCADE, so deleting a question that is a secondary link of a COMMITTED dataset silently strips a link row from a dataset the service elsewhere declares immutable, invalidating its content-addressed commit_hash'; prescribed fix: 'query datasets and sessions referencing the question (and committed datasets holding it as a question link)') is NOT fixed. The guard only compares primary_question_id; it never inspects dataset.question_links. I reproduced it: created a COMMITTED dataset with a primary and a secondary question, deleted the secondary question via api.delete_question — the delete succeeded, the dataset_question_links row was CASCADE-deleted (db_models.py:177-181; alembic 0002:121; FKs now enforced via PRAGMA foreign_keys=ON, db.py:40), the reloaded committed dataset lost the link (links reconstructed from the link table, sqlalchemy_repository_parts/datasets.py:34-53), and _compute_commit_hash(dataset.commit_manifest) no longer matched the stored commit_hash (hash payload includes question_links, services/shared.py:504-524: output 'HASH MATCH: False'). No bead tracks this remainder: closed bead lab-tracker-cq5.2 describes only the primary_question_id scope, the cq5 epic's open children (cq5.5-cq5.10) cover goals/membership/migrations, and grepping .beads/issues.jsonl for dataset_question_links/secondary-link/committed-dataset-corruption finds no tracking issue — so this is a gap where the closed fix does not resolve the full original finding.

A closely analogous untracked path: claim_questions.question_id is ondelete=CASCADE (db_models.py:592-596, alembic/versions/0021_claim_questions.py:26), so delete_question also silently severs claim 'answers' provenance edges with no guard or audit — same class as cq5.4 (delete_analysis stripping claim support links, fixed for analyses but not for questions).

**Test coverage:** tests/test_core_api.py:691 test_delete_question_rejects_primary_dataset_and_session_references covers the two fixed paths (dataset primary, session primary) and passes. No test covers deleting a question that is a secondary dataset question link or a claim 'answers' link; note the test harness (tests/api_helpers.py:17-23) builds its own engine without the PRAGMA foreign_keys=ON listener from db.py, so DB-level cascade/FK behavior is not exercised by any of these tests.

**New issue found during verification** [high/data-integrity] `src/lab_tracker/services/question_service.py:386` — delete_question still silently corrupts committed datasets via secondary question links (unfixed remainder of H11)

_ensure_question_not_primary_reference only checks dataset.primary_question_id and session.primary_question_id. A question that is a SECONDARY question link of a COMMITTED dataset can still be deleted: dataset_question_links.question_id has ondelete=CASCADE (db_models.py:177-181, alembic/versions/0002_core_entities.py:121), and with FK enforcement now on (db.py:40, added by cq5.1) the link row is silently removed from a dataset the service declares immutable. Because question_links are reconstructed from the link table (sqlalchemy_repository_parts/datasets.py:34-53) and the content-addressed commit_hash is computed over question_links (services/shared.py:504-524), the committed dataset's stored commit_hash no longer matches its manifest. Reproduced end-to-end: delete succeeded and recomputed hash diverged from stored hash (HASH MATCH: False). This is exactly the path H11 (docs/runs/deep-review-2026-06-12.md:124-130) prescribed guarding ('and committed datasets holding it as a question link'), but bead lab-tracker-cq5.2 was closed covering only primary references and no other bead tracks it.

Fix: Extend the guard to reject deletion when the question appears in any dataset's question_links (query dataset_question_links by question_id, or scan dataset.question_links in the existing project-scoped dataset query), at minimum for COMMITTED datasets; add a test mirroring test_delete_question_rejects_primary_dataset_and_session_references for the secondary-link case.

**New issue found during verification** [medium/data-integrity] `src/lab_tracker/db_models.py:592` — delete_question silently severs claim 'answers' provenance links via claim_questions CASCADE

claim_questions.question_id is ondelete=CASCADE (db_models.py:592-596; alembic/versions/0021_claim_questions.py:26). delete_question performs no check for claims answering the question, so deleting it silently removes answers_question_ids rows from every claim that cites it, with no error, warning, or audit trail. This is the same provenance-loss class that cq5.4 fixed for delete_analysis (claims losing support links via claim_analyses CASCADE), but the question-side equivalent was never guarded or tracked — no bead in .beads/issues.jsonl mentions claim_questions cascade on question delete, and the cq5 epic's open children cover goals/membership/migrations only.

Fix: Either extend the delete_question guard to reject deletion while claims answer the question (mirroring the delete_analysis guard from cq5.4), or explicitly document/audit the link removal; add a test asserting the chosen behavior.

### lab-tracker-cq5.3: FIXED (high confidence)

The fix in c382cfe adds DatasetService._ensure_dataset_can_be_deleted (src/lab_tracker/services/dataset_service.py:268-290), invoked from delete_dataset (line 263) before the unit_of_work delete (line 265). All three guards from the original finding exist and work:

1. COMMITTED check: line 269 `if dataset.status != DatasetStatus.STAGED:` raises ValidationError "Only staged, unreferenced datasets can be deleted; archive committed datasets." This blocks COMMITTED and ARCHIVED. No bypass via status downgrade: _DATASET_STATUS_TRANSITIONS (src/lab_tracker/services/shared.py:139-147) only allows COMMITTED->{COMMITTED, ARCHIVED} and ARCHIVED->{ARCHIVED}, so a committed dataset can never return to STAGED.

2. claim_datasets check: lines 273-281 call repository.query_claims(dataset_id=...), which joins ClaimDatasetModel on dataset_id (claims query in src/lab_tracker/sqlalchemy_repository_parts/analyses.py:294-303, `stmt.join(ClaimDatasetModel, ...).where(ClaimDatasetModel.dataset_id == str(dataset_id))`). No project filter, so cross-project claim references are also caught.

3. analysis_datasets check: lines 282-290 call repository.query_analyses(dataset_id=...), joining AnalysisDatasetModel.dataset_id (same file, lines 126-135). Claims supported indirectly via analyses are protected transitively because the analysis itself blocks deletion.

query_from_repository (src/lab_tracker/services/base.py:97-103) correctly unwraps the (entities, total) tuple (`entities, _ = loader(...)`), so the truthiness checks are on the list, not the tuple.

No bypass paths: grep shows repository.datasets.delete is called only from DatasetService.delete_dataset (src/lab_tracker/services/dataset_service.py:265); the HTTP route (src/lab_tracker/routes/datasets.py:112) and API facade (src/lab_tracker/api.py:268-269) delegate to it. The route's storage-file cleanup is registered via run_after_commit only after the service call succeeds, so a blocked delete raises before any file deletion (routes/datasets.py:104-120).

Tests verified passing locally: tests/test_core_api.py:729-781 (test_delete_dataset_rejects_committed_and_referenced_datasets) exercises all three rejections (match="Only staged", match="analyses reference", match="claims reference"); tests/test_http_db_integration.py:210-212 asserts HTTP DELETE of a committed dataset returns 422 and the dataset still exists; tests/test_dataset_file_routes.py:468-471 confirms the staged, unreferenced happy path still deletes (no over-blocking regression). Ran `pytest tests/test_core_api.py -k "delete_dataset or committed_dataset or delete_question"` (3 passed) and `pytest tests/test_dataset_file_routes.py tests/test_http_db_integration.py` (30 passed).

One residual low-severity gap noted in newIssues: the dataset_id FKs on claim_datasets and analysis_datasets still declare ondelete="CASCADE" (src/lab_tracker/db_models.py:523-527 and 562-566), so the guard is purely application-level and a concurrent request could attach a claim/analysis between the check and the delete commit. This is an edge concurrency case, not a regression, and does not change the FIXED verdict for supported flows.

**Test coverage:** tests/test_core_api.py:729 test_delete_dataset_rejects_committed_and_referenced_datasets (covers committed, analysis-referenced, and claim-referenced rejection); tests/test_http_db_integration.py:210-212 (HTTP-level 422 for committed dataset delete, dataset survives); tests/test_dataset_file_routes.py:432-471 test_dataset_delete_removes_attached_files_from_storage (staged unreferenced delete still succeeds). All run and passing locally.

**New issue found during verification** [low/data-integrity] `src/lab_tracker/services/dataset_service.py:268` — Dataset delete reference checks are check-then-act; DB FKs still CASCADE so a concurrent claim/analysis link can be silently dropped

_ensure_dataset_can_be_deleted queries claims/analyses in the request session before delete_dataset commits the delete in a separate unit_of_work. The dataset_id foreign keys on claim_datasets (db_models.py:562-566) and analysis_datasets (db_models.py:523-527) still declare ondelete="CASCADE", so the invariant is enforced only at the application layer. Under concurrent requests (Postgres READ COMMITTED), a claim or analysis linked to the dataset after the check but before the delete commits would still have its join rows cascaded away, silently losing evidence -- the exact failure mode of the original finding, now narrowed to a race window.

Fix: Change the dataset_id FKs on claim_datasets and analysis_datasets to ondelete="RESTRICT" (with an Alembic migration) so the database itself rejects deleting a referenced dataset, making the service-level ValidationError the friendly path and the FK the hard guarantee.

### lab-tracker-cq5.4: FIXED (high confidence)

The fix in c382cfe matches the prescribed invariant and holds up under adversarial probing.

GUARD LOGIC: src/lab_tracker/services/analysis_service.py:178-184 — delete_analysis now calls self._ensure_analysis_can_be_deleted(analysis) before the unit_of_work delete. The guard (lines 244-264) queries repository.query_claims(analysis_id=..., limit=None, offset=0), skips claims with status == ClaimStatus.PROPOSED, computes remaining_analysis_ids excluding the analysis being deleted, and raises ValidationError("Analysis cannot be deleted while it is the last support link for a non-proposed claim.") when "not claim.supported_by_dataset_ids and not remaining_analysis_ids". ClaimStatus is {PROPOSED, SUPPORTED, REJECTED} (src/lab_tracker/models.py:106-109), so both SUPPORTED and REJECTED block — exactly the "non-PROPOSED" invariant in the finding.

QUERY CORRECTNESS: SQLAlchemyClaimRepository.query (src/lab_tracker/sqlalchemy_repository_parts/analyses.py:304-313) joins ClaimAnalysisModel on claim_id and filters analysis_id; limit=None applies no LIMIT (common.py:34-39), so all matching claims are checked. claims_from_rows hydrates supported_by_dataset_ids/supported_by_analysis_ids from the link tables (analyses.py:204-217). The session is flushed before querying (analyses.py:284).

COVERAGE OF ALL PATHS: the only callers of analysis deletion are LabTrackerAPI.delete_analysis (src/lab_tracker/api.py:343-344) and the HTTP route (src/lab_tracker/routes/analyses.py:126-151), both of which go through the guarded service method; repository.analyses.delete is invoked nowhere else (grep over src). There is exactly one repository implementation (SQLAlchemyLabTrackerRepository). The route collects visualization asset storage ids BEFORE calling delete_analysis (routes/analyses.py:133-142) and only queues file deletions after, so a guard rejection (ValidationError -> HTTP error via routes/errors.py:24-25) deletes no files. The check and the delete use the same request-scoped repository/session (services/base.py:24-29, 97-103), so there is no intra-request TOCTOU.

ADJACENT ESCAPES CLOSED: a PROPOSED claim whose only analysis link cascades away cannot later be promoted — update_claim re-runs _ensure_claim_support_links (claim_service.py:160-162; shared.py:279-285 raises for SUPPORTED with no datasets and no analyses). commit_analysis-created SUPPORTED claims are also caught by the guard (verified by probe).

ADVERSARIAL PROBE: I ran a 5-case probe against an engine configured like production (PRAGMA foreign_keys=ON, matching src/lab_tracker/db.py:35-44 added in the same commit): (1) PROPOSED claim does not block, link cascades, promotion afterwards is rejected; (2) two-analysis support — first delete allowed, deleting the last survivor blocked; (3) REJECTED claim with sole analysis support blocks delete; (4) dataset support remaining -> delete allowed; (5) commit_analysis-created SUPPORTED claim blocks delete. All passed.

ONE GAP FOUND (reported in newIssues, not a production defect): the same probe run against tests/api_helpers.py's engine — which omits the FK pragma (tests/api_helpers.py:17-22) — FAILED cases 1 and 2: with FK enforcement off, the claim_analyses row for a deleted analysis dangles, the dangling id counts as "remaining support", and the guard then permits deleting the genuinely last support of a SUPPORTED claim. This cannot happen on production engines (db.py sets the pragma for SQLite; the Alembic schema has ondelete="CASCADE" on claim_analyses.analysis_id, alembic/versions/0004_claims_visualizations.py:56, enforced natively on Postgres). But it means the regression test runs under semantics no supported runtime uses, and the suite cannot catch cascade-dependent guard bugs. Bead lab-tracker-cq5.1 (closed) fixed only src/lab_tracker/db.py get_engine; the test-helper engines (api_helpers.py, test_auth_routes.py:22, test_persistence_flows.py, test_sqlalchemy_repository.py:45, etc.) are not tracked in the report (H4) or beads — I checked.

**Test coverage:** tests/test_analysis_claims_visualizations.py::test_delete_analysis_rejects_last_support_for_non_proposed_claim (lines 215-243, added in c382cfe) covers the core block case: COMMITTED analysis as sole support of a SUPPORTED claim -> delete raises ValidationError matching "last support link". The file passes (6 passed). Coverage is minimal: no test that deletion SUCCEEDS when the claim is PROPOSED or when other support remains, no REJECTED-claim case, and the test harness engine has SQLite FK enforcement off, so the cascade interactions the guard depends on are not exercised by the suite (see newIssues).

**New issue found during verification** [medium/testing] `tests/api_helpers.py:17` — Test-helper SQLite engines omit PRAGMA foreign_keys=ON, so cascade-dependent guards are untestable and the cq5.4 guard is bypassable in the test environment

Bead lab-tracker-cq5.1 enabled PRAGMA foreign_keys=ON in the production engine (src/lab_tracker/db.py:35-44), but tests/api_helpers.py repository_backed_api() builds its own engine without the pragma, as do test_auth_routes.py:22, test_device_auth.py:27, test_persistence_flows.py, test_observability.py, test_service_composition.py:46 and test_sqlalchemy_repository.py:45. With FK enforcement off, ON DELETE CASCADE on claim_analyses never fires in tests. I verified this materially diverges from production: running the new delete_analysis guard scenarios against the api_helpers-style engine, (a) deleting an analysis that supports a PROPOSED claim leaves a dangling claim_analyses row (the claim still reports the deleted analysis in supported_by_analysis_ids), and (b) for a SUPPORTED claim backed by analyses A and B, deleting A then B succeeds — the dangling A row counts as 'remaining support' in _ensure_analysis_can_be_deleted, so the SUPPORTED claim silently loses its last real support, the exact bug lab-tracker-cq5.4 was meant to prevent. The same scenarios pass with the pragma enabled. The new regression test in tests/test_analysis_claims_visualizations.py therefore runs under DB semantics no supported runtime uses, and the suite is structurally unable to catch cascade-dependent regressions in any delete guard.

Fix: Have tests/api_helpers.py (and the other test engine factories) reuse lab_tracker.db.get_engine or attach the same _configure_sqlite_engine connect listener so test SQLite engines enforce foreign keys, then add a guard test covering the sequential-delete case (claim supported by two analyses: deleting the first succeeds, deleting the survivor is rejected).

### lab-tracker-fee.1: FIXED (high confidence)

Every mutating path named in the original finding now enforces ProjectAuthorizationPolicy.require_contributor, verified by reading current code, the c382cfe diff, and live adversarial HTTP probes.

Service-layer gates (all verified in current code, mirroring NoteService):
- Questions: create_question (src/lab_tracker/services/question_service.py:89), update_question (:183), refactor_question (:242), delete_question (:380) all call self.authorization.require_contributor(<project_id>, actor=actor).
- Sessions: create_session (src/lab_tracker/services/session_service.py:86), update_session (:144), delete_session (:163), register_acquisition_output (:178), delete_acquisition_output (:232), promote_operational_session (:245), promote_operational_session_to_dataset (:275, plus a second check inside create_dataset).
- Datasets: create_dataset (src/lab_tracker/services/dataset_service.py:91), update_dataset (:174), delete_dataset (:262).
- Dataset files (DB-direct route handlers): upload uses ensure_project_contributor (src/lab_tracker/routes/dataset_files.py:74), delete uses ensure_project_contributor (:208); list (:153) and download (:176) keep ensure_project_read. git show c382cfe confirms the old route-level require_role(actor, WRITE_ROLES)/ensure_project_read gates were replaced.
- All HTTP routes pass actor into the services (e.g. routes/questions.py:44-52, routes/sessions.py:51-57/135-144/161-191, routes/datasets.py:42-51/83-93/99-112), and the policy (src/lab_tracker/services/project_authorization.py:77-87) admits only ADMIN (has_global_write) or CONTRIBUTOR/OWNER membership; membership_role raises AuthError for actor=None.
- Non-route callers are covered: GraphPatchApplier forwards actor to the same gated methods (src/lab_tracker/services/graph_draft_applier.py:121-159, 230-266); AcquisitionOutputWatcher forwards its actor (src/lab_tracker/acquisition_watcher.py:85-91); the MCP server and the new evidence-inbox client (faceb89) go through the HTTP API; provenance_ingestion.py (e9342b9) contains only pure helpers with no repository writes. Auth-disabled mode injects a local ADMIN actor (src/lab_tracker/app_parts/middleware.py:50-51, 69-71), so no regression there.

Live adversarial verification (temporary pytest probe, run then deleted): a project VIEWER member and a global EDITOR non-member were both denied (401) on all 15 mutating paths — question create/PATCH/refactor/DELETE, session create/PATCH/outputs-POST/promote/promote-to-dataset/DELETE, dataset create/PATCH/DELETE, dataset file upload/DELETE — with no state mutated (question text, session status, dataset status unchanged afterward). Viewer GETs returned 200 for question, session, dataset, dataset files list, session outputs list, and refactors list. An ADMIN without membership could create/update/delete. 2/2 probe tests passed; tests/test_project_collaboration.py (3 passed) and tests/test_http_db_integration.py + tests/test_service_composition.py (25 passed) also pass.

Known-issue cross-check: remaining WRITE_ROLES usage is only in project_service.py (project create/update/delete, outside fee.1 scope); analysis/claim/visualization gating was fee.2 (also done in c382cfe); membership PATCH upsert (fee.3), user enumeration (fee.4), and list-scoping test gaps (lab-tracker-aai.2) are already tracked beads and not re-reported.

**Test coverage:** tests/test_project_collaboration.py::test_project_contributor_can_use_core_write_routes (added in c382cfe) covers: viewer member denied question create (401); contributor member succeeds on question/session/dataset create, dataset file upload, analysis/claim/visualization create, and visualization file upload. tests/test_project_collaboration.py::test_project_membership_scopes_reads_and_contributor_notes covers read scoping. Not covered by committed tests: deny paths for question PATCH/refactor/DELETE, session PATCH/DELETE/outputs/promote/promote-to-dataset, dataset PATCH/DELETE, dataset file DELETE, and the cross-project global-EDITOR case — I verified all of these manually with a live probe (all correctly denied), but no regression test locks them in.

**New issue found during verification** [low/testing] `tests/test_project_collaboration.py:122` — Deny-path regression tests missing for most fee.1 write gates

The fix's only denial assertion is a project viewer being rejected on POST /questions (tests/test_project_collaboration.py:142-151). None of the other 14 gated mutating paths have a denial test: question PATCH/refactor/DELETE, session PATCH/DELETE, session outputs POST, promote, promote-to-dataset, dataset PATCH/DELETE, dataset file upload/delete, nor the cross-project case (global EDITOR with no membership) that was the core of the original IDOR finding. A future refactor that drops one require_contributor call (e.g. during a service split) would pass the suite. I verified all paths are currently denied via a live probe, so this is a coverage gap, not a defect. Open bead lab-tracker-aai.2 only covers list-endpoint read scoping, so this is not already tracked.

Fix: Parametrize a denial test over the full mutation matrix (method, url) for two actors: a project viewer member and a global EDITOR non-member, asserting 401 and that the record is unchanged; mirror the probe matrix used in this verification.

### lab-tracker-fee.2: FIXED (high confidence)

VERIFIED FIXED. I read the current code, the c382cfe diff, and probed the running services adversarially.

Service-layer enforcement (all 10 mutating methods now call ProjectAuthorizationPolicy.require_contributor with the entity's project_id):
- AnalysisService (src/lab_tracker/services/analysis_service.py): create_analysis line 76 ("self.authorization.require_contributor(project_id, actor=actor)"), update_analysis line 160, delete_analysis line 180, commit_analysis line 196. The constructor takes authorization: ProjectAuthorizationPolicy (lines 48, 55). require_role/WRITE_ROLES imports are gone.
- ClaimService (src/lab_tracker/services/claim_service.py): create_claim line 67, update_claim line 131, delete_claim line 172; authorization injected at lines 42, 49.
- VisualizationService (src/lab_tracker/services/visualization_service.py): create_visualization line 46, update_visualization lines 101-102, delete_visualization lines 130-131 — update/delete correctly resolve the project via the owning analysis ("analysis = self.analyses.get_analysis(visualization.analysis_id); self.authorization.require_contributor(analysis.project_id, actor=actor)").

commit_analysis inline creation: inline claims and visualizations are created by delegating to self.claims.create_claim(..., actor=actor) and self.visualizations.create_visualization(..., actor=actor) (analysis_service.py:209-235), each of which re-runs require_contributor on the same project. Probed: non-member EDITOR calling commit_analysis with inline ClaimInput is denied with AuthError.

Visualization file upload/delete: upload route now calls ensure_project_contributor(request, analysis.project_id) (src/lab_tracker/routes/visualizations.py:122, replacing the pre-fix require_role+ensure_project_read combo per git show c382cfe). There is no separate file-delete endpoint; the asset is removed via DELETE /visualizations/{viz_id}, whose storage cleanup only runs after the service's gated delete_visualization succeeds (routes/visualizations.py:230). Dataset file upload/delete were also moved to ensure_project_contributor (routes/dataset_files.py:74, 208) in the same commit.

Wiring and coverage of all entry points: LabTrackerAPI constructs all three services with authorization=self.project_authorization (src/lab_tracker/api.py, c382cfe diff at the AnalysisService/ClaimService/VisualizationService constructors). All HTTP routes pass actor= to the service methods (routes/analyses.py:57,101,115,142; routes/claims.py:37-46,85-96,102; routes/visualizations.py:68,213,230). MCP tools reach these mutations only through the HTTP API client (src/lab_tracker/mcp_api_client.py:491-541), so they inherit the gates. git grep confirms no direct repository saves/deletes of claims/analyses/visualizations outside the three services. Fail-closed semantics confirmed: require_contributor with actor=None raises AuthError via membership_role (src/lab_tracker/services/project_authorization.py:57-58, 77-87); only global ADMIN bypasses membership (has_global_write, lines 31-32), matching the established pattern in note/goal/graph-draft services.

Runtime verification: ran tests/test_project_collaboration.py, tests/test_project_authorization_policy.py, tests/test_analysis_claims_visualizations.py — 14 passed. I additionally wrote a 19-scenario adversarial probe against repository_backed_api covering the exact escalations from the original finding: global-EDITOR non-member denied on create/update/delete for all three entity types plus commit_analysis (with and without inline claims); project-VIEWER member denied on update_claim, delete_visualization, commit_analysis, create_claim; unauthenticated denied; and the intended positive case (project CONTRIBUTOR with only global VIEWER role) allowed including commit_analysis with inline claim+visualization. All 19 behaved correctly.

Minor non-findings: (1) HTTP-level negative tests cover viewer denial only for /questions (tests/test_project_collaboration.py:142-152), not specifically for /analyses, /claims, /visualizations — but policy denial semantics are unit-tested in tests/test_project_authorization_policy.py and I confirmed runtime behavior; broader authorization test coverage is already tracked under open bead lab-tracker-aai.2/epic lab-tracker-aai, so per the known-issues rule I am not filing it. (2) POST /claims and POST /analyses routes add no route-level ensure_project_contributor, but the original finding's preferred fix ("better, enforce ... inside the three services so every caller is covered", docs/runs/deep-review-2026-06-12.md:786) is implemented, and the service check covers every caller. (3) The new _ensure_analysis_can_be_deleted guard (analysis_service.py:244-264, added in the same commit for a different bead) is correct as far as I probed: claim_analyses link rows have ondelete=CASCADE (db_models.py:577-579) so allowed deletions leave no dangling support references.

**Test coverage:** tests/test_project_collaboration.py::test_project_contributor_can_use_core_write_routes (contributor-positive HTTP paths for analyses, claims, visualizations including visualization file upload, plus viewer denial on /questions); tests/test_project_authorization_policy.py (require_contributor denial for viewer members, non-members, and unauthenticated actors; admin bypass); tests/test_analysis_claims_visualizations.py::test_delete_analysis_rejects_last_support_for_non_proposed_claim (new delete guard). Gap: no HTTP-level negative tests assert non-contributor denial specifically on /analyses, /claims, /visualizations write routes — verified manually via a 19-scenario service-layer probe (all correct); broader auth test coverage is tracked in open bead lab-tracker-aai.2.

### lab-tracker-zl9.1: FIXED (high confidence)

The defect (validator only rejected DEFAULT_AUTH_SECRET_KEY when environment != local, leaving forgeable admin tokens when auth was enabled in local) is fully resolved. Evidence: (1) src/lab_tracker/config.py:59 now reads `if self.is_auth_enabled() and auth_secret_key in INSECURE_AUTH_SECRET_KEYS:` — the environment qualifier is gone, so rejection fires whenever auth is enabled, in any environment; confirmed against the pre-fix code via `git show c382cfe -- src/lab_tracker/config.py`, which shows the old `not is_local and self.is_auth_enabled() and self.auth_secret_key == DEFAULT_AUTH_SECRET_KEY` condition removed. (2) The fix also strips the secret before comparison (config.py:52-53), so padded placeholders like "  dev-only-change-me  " are caught (verified by live probe), and extends the check to a denylist INSECURE_AUTH_SECRET_KEYS (config.py:9-14) covering the docker-compose placeholder "replace-with-a-strong-secret" plus "change-me"/"changeme". (3) Local-no-auth still works: config.py:54 only raises for non-local+auth-disabled, and a live probe confirmed Settings(environment="local") with the default secret constructs with is_auth_enabled()==False; the original attack path (LAB_TRACKER_ENVIRONMENT=local + LAB_TRACKER_AUTH_ENABLED=true + default secret, set via env) is rejected (probe #6). (4) No bypass paths: the only TokenService construction is src/lab_tracker/app_parts/runtime.py:56-59 fed from validated Settings; grep found no model_construct or direct env reads of the secret in src; all Settings instantiations (app.py:23, db.py:23, routes/*) go through Settings()/get_settings(), which run the model_validator. Residual non-defects checked and dismissed: an empty/whitespace secret passes the config validator ("" is not in the denylist) but the app fails closed at startup because TokenService.__init__ raises "auth_secret_key must not be empty." (src/lab_tracker/auth.py:230-232) — pre-existing behavior, identical before the fix, not a regression; case-variant placeholders (e.g. "DEV-ONLY-CHANGE-ME") pass the case-sensitive denylist, but that requires the operator to deliberately invent a variant and is not an attacker-controllable path — minimum-entropy enforcement was already noted as optional hardening in the original report (docs/runs/deep-review-2026-06-12.md, H1 fix text), so not re-reported.

**Test coverage:** tests/test_config.py — all 8 tests pass (ran .venv\Scripts\python.exe -m pytest tests/test_config.py -q). Coverage of the fix: test_local_environment_rejects_default_auth_secret_when_auth_enabled (lines 27-32, the exact original defect path), test_local_environment_can_enable_auth_with_custom_secret (35-41), test_local_environment_allows_default_auth_secret (19-24, no-auth backward compat), test_non_local_environment_rejects_default_auth_secret (60-64), test_non_local_environment_rejects_placeholder_auth_secret (67-72), test_non_local_environment_accepts_custom_auth_secret (75-81), test_non_local_environment_rejects_disabled_auth (84-90).

### lab-tracker-zl9.2: PARTIAL (high confidence)

The fix addresses two of the three placeholder paths named in the original finding (deep-review-2026-06-12.md H1) but explicitly omits the third, which the finding called out by name.

What is fixed:
1. docker-compose.yml:15 now reads `LAB_TRACKER_AUTH_SECRET_KEY: ${LAB_TRACKER_AUTH_SECRET_KEY:?Set LAB_TRACKER_AUTH_SECRET_KEY to a strong secret}` (changed in c382cfe from `:-replace-with-a-strong-secret`). The `:?` form makes compose fail on an unset or empty variable. Verified via `git show c382cfe -- docker-compose.yml`.
2. src/lab_tracker/config.py:9-14 adds `INSECURE_AUTH_SECRET_KEYS = {DEFAULT_AUTH_SECRET_KEY, "replace-with-a-strong-secret", "change-me", "changeme"}` and config.py:59 rejects any of these whenever auth is enabled (now including local-with-auth-enabled, stricter than before). I executed the validator: production + "replace-with-a-strong-secret" raises ValidationError as expected.

What is NOT fixed:
The original finding H1 (docs/runs/deep-review-2026-06-12.md:24) states "The same problem applies to .env.example line 7 ('replace-with-a-strong-random-secret'), which also passes the check", and the prescribed fix (line 26) says to denylist "both 'replace-with-a-strong-secret' variants". The implemented denylist contains only the compose variant; `.env.example:7` still ships `LAB_TRACKER_AUTH_SECRET_KEY=replace-with-a-strong-random-secret` (unchanged since f71ea24 — `git diff f71ea24..HEAD -- .env.example` is empty) alongside `LAB_TRACKER_ENVIRONMENT=production` (line 3) and `LAB_TRACKER_AUTH_ENABLED=true` (line 10). I executed the validator with production + "replace-with-a-strong-random-secret": it printed "VALIDATOR ACCEPTED: 'replace-with-a-strong-random-secret' auth_enabled= True". The end-to-end exploit survives: a user who copies .env.example to .env (docker compose reads .env for interpolation, so the `:?` requirement is satisfied by the placeholder; pydantic also loads .env directly via `env_file=".env"` at config.py:68) boots production with a repo-public HMAC-SHA256 signing key (used by TokenService at src/lab_tracker/auth.py:227-252, wired at src/lab_tracker/app_parts/runtime.py:56-59) on port 8000 published on all interfaces (docker-compose.yml:18 "8000:8000"), allowing anyone who knows the repo to forge admin bearer tokens — the exact attack in the original finding. The bead lab-tracker-zl9.2 is closed, so this gap is not tracked anywhere.

Edge cases checked and clean: empty/whitespace-only secret passes the config validator (stripped "" is not in the denylist) but fails closed at startup because TokenService.__init__ raises "auth_secret_key must not be empty." (src/lab_tracker/auth.py:230-232), so no silent insecure state. Dockerfile sets no secret default (grep for SECRET/ENVIRONMENT returned nothing). Production with AUTH_ENABLED=false still raises (config.py:54-58, test_config.py:84-90).

**Test coverage:** tests/test_config.py:67-72 (test_non_local_environment_rejects_placeholder_auth_secret) covers the compose literal "replace-with-a-strong-secret" in production; tests/test_config.py:60-64 covers the default secret; tests/test_config.py:27-32 covers local+auth-enabled rejecting the default. No test covers the .env.example literal "replace-with-a-strong-random-secret" — adding one would fail against current code. No test exercises the compose `:?` requirement (compose interpolation is not unit-testable here; acceptable).

**New issue found during verification** [high/security] `src/lab_tracker/config.py:9` — Secret denylist omits the .env.example placeholder 'replace-with-a-strong-random-secret', leaving the original token-forgery path open

The fix for lab-tracker-zl9.2 added INSECURE_AUTH_SECRET_KEYS but only denylists the docker-compose variant 'replace-with-a-strong-secret'. The original finding (docs/runs/deep-review-2026-06-12.md H1) explicitly required rejecting "both 'replace-with-a-strong-secret' variants", and .env.example:7 still ships LAB_TRACKER_AUTH_SECRET_KEY=replace-with-a-strong-random-secret together with LAB_TRACKER_ENVIRONMENT=production and LAB_TRACKER_AUTH_ENABLED=true. A user following the standard workflow (copy .env.example to .env, run docker compose up) satisfies the new compose ':?' requirement with the placeholder (compose interpolates from .env; pydantic also loads .env via env_file at config.py:68) and the validator accepts it, so the repo-public string becomes the live HMAC-SHA256 signing key for bearer tokens (auth.py TokenService) on a service published on all interfaces (docker-compose.yml:18). Anyone who knows the repo can forge admin tokens — the exact attack the closed bead was supposed to eliminate. Empirically verified: Settings(_env_file=None) with environment=production and that secret validates successfully.

Fix: Add 'replace-with-a-strong-random-secret' to INSECURE_AUTH_SECRET_KEYS (and consider a substring/prefix check for 'replace-with' style placeholders or a minimum-length requirement, e.g. reject secrets under 16 chars when auth is enabled). Also change .env.example to leave LAB_TRACKER_AUTH_SECRET_KEY empty so the compose ':?' guard fires instead of being satisfied by a placeholder. Add a test mirroring tests/test_config.py:67-72 for the .env.example literal.

### lab-tracker-bsk.1: PARTIAL (high confidence)

FIXED paths — upload-queue.js: commit c382cfe replaced both `await runRequest(tx)` calls with `await txDone(tx)`. Current code (src/lab_tracker/frontend_src/shared/upload-queue.js) has a correct helper at lines 41-47 (`tx.oncomplete = () => resolve(); tx.onerror = () => reject(tx.error); tx.onabort = () => reject(tx.error || new Error("IndexedDB transaction aborted."))`), used in add() at line 57, remove() at line 77, and the new update() at line 93. All remaining runRequest() calls in that file take IDBRequest objects only (store.add/get/put/delete, getAll), which do fire success events. The handler-attachment ordering (await request success, then attach tx.oncomplete in the microtask continuation) is the standard safe idiom — the complete event is dispatched in a later macrotask, so no missed-event race. The committed bundle src/lab_tracker/frontend/app.js was rebuilt in c382cfe and contains the txDone code (grep for "transaction aborted" matches), so the served app gets the fix.

NOT FIXED path — share-target-inbox.js: the original finding H7 (docs/runs/deep-review-2026-06-12.md lines 88-90) explicitly included this file: "migrateIncomingShares (shared/share-target-inbox.js:116 ... and its own remove() at line 54 has the same `await runRequest(tx)` bug)" and prescribed "use it in upload-queue.js add()/remove() and share-target-inbox.js remove()". src/lab_tracker/frontend_src/shared/share-target-inbox.js line 54 still reads `await runRequest(tx);` inside createIndexedDbShareStorage().remove() (lines 49-58), where runRequest (lines 31-36) resolves only via request.onsuccess — IDBTransaction never fires success, so the promise never settles. `git log f71ea24..HEAD -- src/lab_tracker/frontend_src/shared/share-target-inbox.js` shows zero commits; only upload-queue.js and its test changed in shared/. No open bead covers this: lab-tracker-bsk.7 is about text/URL share persistence and sw.js failure signaling, not the transaction await, and bsk.1 (which the report maps to H7) is CLOSED. Reachable impact: mobile-capture.jsx:351 calls migrateIncomingShares with the default real-IndexedDB storage; the loop's `await storage.remove(share.id)` (share-target-inbox.js:122, or :109 for file-less shares) hangs after the first share, so with multiple pending OS shares only one is enqueued per page boot, the "shared capture queued" flash (mobile-capture.jsx:356-360) never appears, and the immediate queue.drain() at mobile-capture.jsx:361 never runs. The bundle app.js also contains the share-inbox code built from this unfixed source. sw.js's share-inbox writer (frontend/sw.js:143-145) resolves on the add request's onsuccess, not the transaction, so it is not affected.

**Test coverage:** upload-queue.test.jsx gained "waits for IndexedDB transaction completion when adding records" (lines ~223-272), which correctly simulates the real event model: the fake transaction object never fires a success event, the test asserts the add() promise stays unsettled until tx.oncomplete is assigned and manually invoked, then resolves to the key and closes the db. Verified passing: `npx vitest run` on upload-queue.test.jsx + share-target-inbox.test.jsx -> 17/17 passed. Gaps: only the add() path is exercised against the fake IndexedDB; remove() and update() rely on the shared txDone helper but have no real-adapter test. share-target-inbox.test.jsx uses createMemoryShareStorage exclusively, so the unfixed IndexedDB remove() hang is invisible to CI — exactly the test blind spot the original finding called out.

**New issue found during verification** [high/bug] `src/lab_tracker/frontend_src/shared/share-target-inbox.js:54` — share-target-inbox remove() still awaits IDBTransaction success event that never fires (path named in closed bead bsk.1)

Closed bead lab-tracker-bsk.1 maps to deep-review finding H7, whose prescribed fix explicitly says to use the txDone helper 'in upload-queue.js add()/remove() and share-target-inbox.js remove()'. Commit c382cfe fixed only upload-queue.js; share-target-inbox.js was not touched anywhere in f71ea24..HEAD. createIndexedDbShareStorage().remove() still does `await runRequest(tx)` where runRequest resolves via request.onsuccess — IDBTransaction has no success event, so the promise never settles with real IndexedDB. migrateIncomingShares (called from mobile-capture.jsx:351 with this default storage) therefore hangs at its first `await storage.remove(share.id)` (line 122, or line 109 for file-less shares): with multiple pending OS share-target captures only the first is enqueued per page boot, the 'shared capture queued' flash never shows, the immediate queue.drain() at mobile-capture.jsx:361 never runs, and the inbox record is never confirmed removed (the delete itself auto-commits, but the loop and caller are stuck). No open bead tracks this; bsk.7 covers a different share-target issue (text/URL shares, sw.js failure signaling). The committed bundle frontend/app.js includes this unfixed code.

Fix: Apply the same fix as upload-queue.js: import or duplicate the txDone(tx) helper (oncomplete/onerror/onabort) and replace `await runRequest(tx)` at line 54 with `await txDone(tx)`; add a fake-event-model test for the real adapter mirroring the new upload-queue add() test, then rebuild the committed bundle. Re-open or re-file the share-inbox portion of lab-tracker-bsk.1.

### lab-tracker-bsk.2: PARTIAL (high confidence)

The core retention logic is genuinely fixed. src/lab_tracker/frontend_src/shared/upload-queue.js now partitions 4xx: PERMANENT_CLIENT_REJECTION_STATUSES = {400, 404, 409, 410, 413, 415, 422} (line 18) are dropped with rejectedStatus surfaced in the drain() result (lines 224-227); all other 4xx including 401/408/429 are kept queued via keepQueuedForRetry (lines 133-144, 229-241) with a bounded cap MAX_RETRY_ATTEMPTS = 5 (line 17) and dropReason "retry_limit" (lines 233-239). 5xx and network errors remain queued without consuming retries (lines 215-217, 243-244). The IndexedDB adapter gained the update() method needed for retry bookkeeping (lines 82-98) using the txDone helper. The committed bundle is current: `npm run build` produced zero git diff under src/lab_tracker/frontend/, and frontend/app.js contains "retry_limit", the status set "400,404,409,410,413,415,422", and "retryCount". 13/13 tests pass in upload-queue.test.jsx.

However, two elements of the original finding are NOT delivered. (1) The bead description ("surface dropped items to the user") and the report prescription (docs/runs/deep-review-2026-06-12.md line 100: "surface dropped items to the user (e.g. via the existing PendingUploadsBadge/subscribe channel) instead of discarding the diagnostics") are unimplemented: every drain() caller discards the result — register-sw.js:71 and :75 (`queue.drain().catch(() => {});`) and mobile-capture.jsx:361 (`return queue.drain().catch(() => undefined);`). The only UI, PendingUploadsBadge (upload-status.jsx:37-48), renders just a pending count, which decrements on a drop exactly as on a success, so a 422-rejected or retry-exhausted capture vanishes with zero user notice. (2) The headline scenario named in the bead — "401 from a token that expired while the capture sat queued offline" — still ends in permanent loss: drain() replays the token frozen at enqueue time (upload-queue.js:206 `const headers = item.token ? { Authorization: Bearer ${item.token} } : {}`), nothing ever refreshes item.token, tokens expire (config.py:25 auth_token_ttl_minutes = 60*12), and drain() runs at boot plus every `online` event (register-sw.js:70-75), so after 5 doomed 401s the capture is deleted — silently, per gap (1) — even if the user has re-authenticated. Open bead lab-tracker-bsk.4 covers session-level refresh in useAuthSession/api layer only, not the queue's stored per-item token, and no other open bead (bsk.5 idempotency, bsk.7 share inbox) tracks either gap, so these are unreported residual defects of the closed bsk.2 fix, not known issues.

**Test coverage:** src/lab_tracker/frontend_src/shared/upload-queue.test.jsx (13 tests, all passing via `npx vitest run`): permanent 422 drop with rejectedStatus (lines 125-139), retryable 401/408/429 kept queued with lastStatus/retryCount (lines 141-157), retry-cap drop with dropReason "retry_limit" after MAX_RETRY_ATTEMPTS drains (lines 159-179), 5xx retention (lines 181-194), network-failure retention (lines 107-123). No test covers user-facing surfacing of dropped items (the behavior does not exist) or replay after token expiry/refresh.

**New issue found during verification** [high/data-integrity] `src/lab_tracker/frontend_src/shared/upload-queue.js:206` — Offline captures with an expired token are still permanently lost: drain() replays the token frozen at enqueue, so 401s can never recover and the retry cap deletes the item

The bsk.2 fix keeps 401 responses queued, but each replay sends the token stored on the item at enqueue time and nothing ever updates it. Access tokens expire (config.py:25, auth_token_ttl_minutes = 60*12, AccessTokenIssuer default 60 in auth.py:230), so any capture queued offline longer than the TTL — e.g. field capture, back online the next day — replays a dead token, gets 401 on every attempt, and after MAX_RETRY_ATTEMPTS=5 drain cycles (drain runs at boot and on every `online` event, register-sw.js:70-75) the capture is deleted. Re-authenticating in the app does not help because item.token in IndexedDB is never refreshed. The exact scenario named in the original finding ("401 from a token that expired while the capture sat queued offline") therefore still ends in permanent data loss; the fix only converts immediate deletion into delayed deletion. Distinct from open bead lab-tracker-bsk.4, which covers session refresh in useAuthSession/api.js only, not the queue's per-item stored token.

Fix: Have drain() accept the current session token (or a getToken callback) and prefer it over item.token for Authorization; alternatively refresh item.token on auth change, and exclude 401 from the retry-count increment so re-auth can rescue queued captures.

**New issue found during verification** [medium/data-integrity] `src/lab_tracker/frontend_src/shared/register-sw.js:71` — Dropped captures are never surfaced to the user: every drain() caller discards results.dropped, so permanent and retry-limit drops are silent

Bead lab-tracker-bsk.2 and the deep-review fix prescription (docs/runs/deep-review-2026-06-12.md line 100) explicitly required surfacing dropped items to the user, e.g. via the PendingUploadsBadge/subscribe channel. The fix added results.dropped to drain()'s return value but no consumer exists: register-sw.js calls `queue.drain().catch(() => {})` at lines 71 and 75, and mobile-capture.jsx:361 calls `return queue.drain().catch(() => undefined)`. The only queue UI, PendingUploadsBadge (upload-status.jsx:37-48), shows a pending count that decrements identically on success and on drop. So a capture rejected with 422 or exhausted after 5 retries disappears with no notification, leaving the user unaware they must re-capture — the same silent-loss UX the original P1 called out, now limited to permanent/exhausted cases. The bead is closed, and no open bead (bsk.4/bsk.5/bsk.7) tracks this surfacing gap.

Fix: Consume drain() results in installOfflineRetry and the mobile-capture migration path: on results.dropped.length > 0, raise a flash/toast or persist a 'failed captures' list shown next to PendingUploadsBadge, including rejectedStatus/dropReason so the user knows to re-capture.

### lab-tracker-bsk.3: FIXED (high confidence)

The fix in c382cfe fully resolves lab-tracker-bsk.3. (1) Source fix: src/lab_tracker/frontend_src/hooks/useAuthSession.js:54-59 now reads `if (token && (err.status === 401 || err.status === 403)) { setToken(""); setFlash("", err.message || "Failed to restore session."); } else if (token) { setFlash("", err.message || "Could not verify the saved session."); }` — the token (and via the sync effect at lines 24-30, localStorage) is cleared only on explicit server rejection. (2) Discriminator correctness verified: src/lab_tracker/frontend_src/shared/api.js:26-32 sets `error.status = response.status` only inside throwApiError, which is invoked only after a server response with !response.ok (api.js:72-74); an offline fetch rejects with a bare TypeError lacking .status, so it can never match the 401/403 branch. (3) Backend exhaustiveness: /auth/me token rejections raise AuthError → 401 (src/lab_tracker/app_parts/middleware.py:42) or forbidden → 403 (middleware.py:47); no other status signals an invalid token, so the discriminator covers all token-rejection cases. (4) The committed bundle served by the API was rebuilt in the same commit (c382cfe diffstat includes src/lab_tracker/frontend/app.js, 14 lines changed) and contains the minified discriminator `r&&(b.status===401||b.status===403)?(o(""),n("",b.message||"Failed to restore session.")):r&&n("",b.message||"Could not verify the saved session.")`. (5) No bypass paths: the only other setToken("") sites are the auth-disabled branch (useAuthSession.js:46, intentional) and explicit logout (line 111, user action); grep found no other localStorage.removeItem(TOKEN_STORAGE_KEY) in frontend_src. Residual non-defect observation: after an offline boot failure the boot effect does not auto-retry on reconnect (deps are [setBusy, setFlash, token]); the user must reload to restore the session, but the token is preserved exactly as the bead required, so this is an enhancement, not a regression.

**Test coverage:** src/lab_tracker/frontend_src/hooks/useAuthSession.test.jsx covers both cases added in c382cfe: "preserves a saved token when boot session restore fails offline" (lines 15-32; stubs global fetch to throw TypeError("Network unavailable"), asserts token retained in hook state and localStorage and flash shows the error) and "clears a saved token when the server rejects it" (lines 34-48; uses installFetchMock + errorResponse("Invalid token.", 401) which flows through the real apiFetch/throwApiError path setting err.status, asserts localStorage token removed and hook token emptied). Ran `npx vitest run src/lab_tracker/frontend_src/hooks/useAuthSession.test.jsx`: 2/2 passed.

### lab-tracker-f6k.1: FIXED (high confidence)

The original defect (bead lab-tracker-f6k.1) was that [tool.setuptools.package-data] in pyproject.toml listed only 4 of 9 files in src/lab_tracker/frontend (index.html, app.js, app.css, styles.css per git show f71ea24:pyproject.toml), so wheels — and the Docker image, which installs via 'uv pip install --system .' per Dockerfile line 17 — lacked sw.js (500), manifest.json and the three icons (404). Commit c382cfe (git diff f71ea24..HEAD -- pyproject.toml) replaced the 4-file list with the glob 'frontend/*' (pyproject.toml:44-47) and added tests/test_packaging.py with test_frontend_package_data_covers_all_bundle_files. Verification against the real artifact: I built the wheel from the current tree (.venv pip wheel, same setuptools.build_meta backend the Dockerfile uses) and inspected it with zipfile — it contains all 9 files: lab_tracker/frontend/{app.css, app.js, icon-180.png, icon-192.png, icon-512.png, index.html, manifest.json, styles.css, sw.js}, exactly matching 'git ls-files src/lab_tracker/frontend'. The directory is flat (no subdirectories), and scripts/build-frontend.mjs writes a single esbuild 'outfile' (src/lab_tracker/frontend/app.js) with sourcemap:false and explicitly rm's app.js.map, so 'frontend/*' covers every current file and any flat file added later. The new test passes locally (1 passed) and runs in CI on Python 3.11 (.github/workflows/ci.yml:14), so its tomllib import is safe and consistent with pre-existing tests (tests/test_lab_tracker_init.py:9, tests/test_mcp_server.py:9). Therefore the defect named in the bead is fully resolved on every named path. The one weakness found: the guard test inspects pyproject patterns with fnmatch instead of building/inspecting the artifact as the prescribed fix specified, and it has a subdirectory blind spot (tests/test_packaging.py:18-19 iterdir/is_file does not recurse; line 24 fnmatch '*' crosses '/'), so a future frontend subdirectory would be silently dropped from the wheel with the test still green. That is a latent gap in the regression guard, not a regression or a live defect, so the verdict is FIXED with the test weakness reported as a new medium testing issue.

**Test coverage:** tests/test_packaging.py::test_frontend_package_data_covers_all_bundle_files (added in c382cfe) asserts every file directly inside src/lab_tracker/frontend is matched by the package-data patterns; it passes locally and runs in CI (Python 3.11). It covers flat-file additions but not subdirectories and does not inspect the built wheel — I verified the actual wheel contents manually (all 9 frontend files present).

**New issue found during verification** [medium/testing] `tests/test_packaging.py:18` — Packaging guard test cannot detect the subdirectory failure mode it exists to prevent

The packaging guard test added in c382cfe does not build or inspect the real wheel (as the original finding's expected fix prescribed); it re-implements pattern matching with fnmatch against the pyproject patterns. This simulation diverges from setuptools' actual glob semantics in exactly the scenario the glob fails: subdirectories. (1) tests/test_packaging.py:18-19 enumerates bundle files with frontend_dir.iterdir() filtered by is_file(), which never recurses, so any file in a future frontend/<subdir>/ is invisible to the test. (2) tests/test_packaging.py:24 uses fnmatch, whose '*' crosses '/' (fnmatch.fnmatch('frontend/assets/x.js', 'frontend/*') returns True), whereas setuptools' glob-based package-data matching for 'frontend/*' matches only the directory entry and ships none of its contents (verified empirically with glob.glob). Consequence: if the frontend bundle ever gains a subdirectory (e.g. esbuild outdir with code splitting, asset/font dirs, sourcemaps in a subfolder), those files are silently excluded from the wheel while the guard test stays green — recreating the exact original 404/500 bug class undetected. Latent today because scripts/build-frontend.mjs emits a single flat outfile (app.js), so the bundle dir is flat.

Fix: Make the test build the wheel (e.g. via 'python -m build --wheel' into a tmpdir, or setuptools' build_py data-file resolution) and assert the wheel's lab_tracker/frontend/* entries equal the recursive contents of src/lab_tracker/frontend (rglob). Alternatively, at minimum use Path.rglob in the test and replace fnmatch with PurePosixPath.match or glob-equivalent semantics, and add 'frontend/**/*' (or per-subdir globs) to package-data so subdirectory files are shipped.

## Confirmed new findings

### HIGH (5)

#### H1. Secret denylist omits the .env.example placeholder 'replace-with-a-strong-random-secret', leaving the original token-forgery path open

`src/lab_tracker/config.py:9` · security · area: fix:lab-tracker-zl9.2

The fix for lab-tracker-zl9.2 added INSECURE_AUTH_SECRET_KEYS but only denylists the docker-compose variant 'replace-with-a-strong-secret'. The original finding (docs/runs/deep-review-2026-06-12.md H1) explicitly required rejecting "both 'replace-with-a-strong-secret' variants", and .env.example:7 still ships LAB_TRACKER_AUTH_SECRET_KEY=replace-with-a-strong-random-secret together with LAB_TRACKER_ENVIRONMENT=production and LAB_TRACKER_AUTH_ENABLED=true. A user following the standard workflow (copy .env.example to .env, run docker compose up) satisfies the new compose ':?' requirement with the placeholder (compose interpolates from .env; pydantic also loads .env via env_file at config.py:68) and the validator accepts it, so the repo-public string becomes the live HMAC-SHA256 signing key for bearer tokens (auth.py TokenService) on a service published on all interfaces (docker-compose.yml:18). Anyone who knows the repo can forge admin tokens — the exact attack the closed bead was supposed to eliminate. Empirically verified: Settings(_env_file=None) with environment=production and that secret validates successfully.

**Fix:** Add 'replace-with-a-strong-random-secret' to INSECURE_AUTH_SECRET_KEYS (and consider a substring/prefix check for 'replace-with' style placeholders or a minimum-length requirement, e.g. reject secrets under 16 chars when auth is enabled). Also change .env.example to leave LAB_TRACKER_AUTH_SECRET_KEY empty so the compose ':?' guard fires instead of being satisfied by a placeholder. Add a test mirroring tests/test_config.py:67-72 for the .env.example literal.

*Verification votes: real/high, real/high, real/high*

#### H2. Rebuilt bundle in c382cfe never reaches installed PWA clients: no cache-bust accompanied the app.js rebuild, so the SW serves the pre-fix bundle indefinitely

`src/lab_tracker/frontend/sw.js:12` · bug · area: gap:Service-worker writer side of the share pipeline (hand-maintained sw.js unchanged while its reader was fixed), committed-bundle/source consistency, and the useAuthSession 401/403 fix verdict

Commit c382cfe rebuilt src/lab_tracker/frontend/app.js with the offline-capture fixes, but `git diff f71ea24..HEAD -- src/lab_tracker/frontend/index.html src/lab_tracker/frontend/sw.js` is empty: index.html still loads `/app/static/app.js?v=17` (index.html:16), sw.js SHELL_ASSETS still pins `"/app/static/app.js?v=17"` (sw.js:16), and CACHE_VERSION is still `"v16"` (sw.js:12). Because sw.js is byte-identical, the browser's SW update check finds nothing to install (register-sw.js registration.update() is a no-op on unchanged bytes), so the activate handler never runs and the `lab-tracker-shell-v16` cache is never invalidated. The fetch handler serves /app/static/* cache-first (sw.js:82-96: `caches.match(request).then((cached) => cached || fetch(request)...)`), so every returning client with the PWA installed keeps executing the OLD app.js — the one with the upload-queue txDone hang, the 4xx-drop bug, and the useAuthSession token wipe — forever. The fixes only reach fresh visitors with no SW. This is precisely the audience inversion that matters: the offline-capture fixes are unreachable by offline/PWA users. Note: deep-review M21 documented this as a *latent* coordination gap ('regenerating app.js without bumping the ?v= leaves sw.js byte-identical'); this diff is the event that actually triggers it, which is why I report it despite M21 — the fix under review effectively did not ship.

**Fix:** As part of the c382cfe follow-up: bump the asset version to ?v=18 in index.html and sw.js SHELL_ASSETS and bump CACHE_VERSION to v17 (any sw.js byte change triggers reinstall + cache cleanup). Longer term, implement M21's single-sourced version injection in scripts/build-frontend.mjs so a bundle rebuild cannot ship without a cache-bust.

*Verification votes: real/high, real/high, real/high*

#### H3. bsk.2 (H8) fix gap: 401-queued items replay the frozen enqueue-time token, then are silently deleted after 5 drains — re-auth can never rescue them

`src/lab_tracker/frontend_src/shared/upload-queue.js:206` · data-integrity · area: fix-collateral-frontend

Also independently reported as: Offline captures with an expired token are still permanently lost: drain() replays the token frozen at enqueue, so 401s can never recover and the retry cap deletes the item

The new retry logic keeps 401 responses queued on the premise that 're-auth or waiting would succeed' (bsk.2/H8). But each queued record freezes the bearer token at enqueue time (upload-queue.js:176, populated from the then-current token in mobile-capture.jsx:594-599 and share-target-inbox.js:113-118), and drain() builds the Authorization header from item.token (line 206) — the user's current token is never consulted and queued items are never re-tokened after login. Access tokens expire after 60 minutes (known: auth.py ttl_minutes=60, no refresh — bsk.4), so any capture that sits queued past expiry 401s on every drain no matter how many times the user re-authenticates. drain() runs at every boot and every 'online' event (register-sw.js:70-76), so MAX_RETRY_ATTEMPTS=5 is exhausted within a few app opens / connectivity flaps, at which point adapter.remove() permanently deletes the only copy of the offline capture (lines 233-239). Net effect: the H8 silent-data-loss scenario the closed P1 bead claims to fix still occurs, merely delayed from drain 1 to drain 5. Note network errors correctly do not burn retries, which makes the stale-401 case the dominant consumer of the cap.

**Fix:** Pass the current token into drain() (e.g. drain({ token }) from callers that have one) and prefer it over item.token, or refresh item.token on successful login; alternatively exclude 401 from the retry-count cap so re-auth has unlimited opportunity to rescue queued captures.

*Verification votes: real/high, real/high, real/high*

#### H4. Offline captures still silently destroyed: 401 retries reuse the stale per-item token and no drain() caller surfaces results.dropped

`src/lab_tracker/frontend_src/shared/upload-queue.js:233` · data-integrity · area: diff-sweep

The fix for H8 (bead lab-tracker-bsk.2, closed) keeps 401/403/408/429 items queued with a retry cap of 5, then deletes them. But each retry sends the token captured at enqueue time (item.token), never the current session token, so once the token that was stored with an offline capture expires (TTL is 12h), every retry is a guaranteed 401 even after the user re-authenticates. After 5 drains (drain fires on every 'online' event in register-sw.js:71,75 and on mobile-capture mount at mobile-capture.jsx:361) the capture is removed from IndexedDB. Worse, H8's prescribed fix explicitly required surfacing dropped items to the user, but every caller discards the drain result: register-sw.js uses `queue.drain().catch(() => {})` and mobile-capture.jsx uses `queue.drain().catch(() => undefined)`, so results.dropped is read by nobody and the pending badge count simply shrinks. Net effect in a fully supported flow (capture offline in the field, come back the next day): the evidence note is permanently lost with no notification — the fix only delays the data loss H8 described.

**Fix:** Have drain() accept a current-token provider (or read the live token from auth state) and prefer it over item.token; only count a retry attempt when the request was actually sent with a token different from the last failing one. Surface results.dropped through the existing subscribe/PendingUploadsBadge channel (e.g. move dropped items to a visible 'failed' store instead of deleting) so users can recover or re-send the capture.

*Verification votes: real/high, real/high, real/high*

#### H5. delete_question guard misses secondary dataset links; FK pragma now silently mutates COMMITTED datasets and invalidates their commit_hash

`src/lab_tracker/services/question_service.py:386` · data-integrity · area: fix-collateral-backend

Also independently reported as: delete_question guard misses CASCADE references: committed dataset question_links and claim answer links are silently deleted, mutating committed manifests under their stored commit_hash; delete_question still silently corrupts committed datasets via secondary question links (unfixed remainder of H11)

The new delete guard added in c382cfe (for closed bead lab-tracker-cq5.2) only blocks deletion when the question is a dataset's or session's PRIMARY question. It does nothing about secondary question links: dataset_question_links.question_id is ON DELETE CASCADE (db_models.py:179, alembic/versions/0002_core_entities.py:121), and answers-links via claim_questions are also CASCADE (alembic/versions/0021_claim_questions.py:26). Before c382cfe these cascades never fired on the default SQLite runtime; the companion fix lab-tracker-cq5.1 (PRAGMA foreign_keys=ON in db.py) now activates them. So DELETE /questions/{id} for a question that is a SECONDARY link of a COMMITTED dataset succeeds and silently strips the link row from a dataset the service declares immutable (update_dataset: 'Committed datasets are immutable.'). Because the content-addressed commit hash covers question_links (services/shared.py:504-524 builds the 'question_links' payload that _compute_commit_hash signs), the committed dataset's stored commit_hash no longer matches its content. The same delete also silently removes answers_question_ids entries from SUPPORTED/REJECTED claims, which are otherwise uneditable ('Only proposed claims can be edited.', claim_service.py:141). The original review explicitly described the secondary-link strip inside the finding that cq5.2 closed, but the shipped guard covers only primary references, and no open bead tracks the remainder — so this is a fix-gap that the FK fix simultaneously turned from a latent dangling reference into active silent corruption.

**Fix:** Extend _ensure_question_not_primary_reference to also reject deletion when the question appears in any COMMITTED dataset's question_links or in any non-proposed claim's answers_question_ids (or explicitly re-link/strip with an audit record). Add a regression test: commit a dataset with a secondary question, delete the question, assert ValidationError and that the recomputed manifest hash still matches commit_hash.

*Verification votes: real/high, real/high, real/high*

### MEDIUM (15)

#### M1. delete_question silently severs claim 'answers' provenance links via claim_questions CASCADE

`src/lab_tracker/db_models.py:592` · data-integrity · area: fix:lab-tracker-cq5.2

claim_questions.question_id is ondelete=CASCADE (db_models.py:592-596; alembic/versions/0021_claim_questions.py:26). delete_question performs no check for claims answering the question, so deleting it silently removes answers_question_ids rows from every claim that cites it, with no error, warning, or audit trail. This is the same provenance-loss class that cq5.4 fixed for delete_analysis (claims losing support links via claim_analyses CASCADE), but the question-side equivalent was never guarded or tracked — no bead in .beads/issues.jsonl mentions claim_questions cascade on question delete, and the cq5 epic's open children cover goals/membership/migrations only.

**Fix:** Either extend the delete_question guard to reject deletion while claims answer the question (mirroring the delete_analysis guard from cq5.4), or explicitly document/audit the link removal; add a test asserting the chosen behavior.

*Verification votes: real/high*

#### M2. sw.js storeIncomingShare resolves on req.onsuccess instead of tx.oncomplete — the exact bug class just fixed in upload-queue.js remains on the writer side; commit-time aborts are silently swallowed

`src/lab_tracker/frontend/sw.js:145` · data-integrity · area: gap:Service-worker writer side of the share pipeline (hand-maintained sw.js unchanged while its reader was fixed), committed-bundle/source consistency, and the useAuthSession 401/403 fix verdict

c382cfe added txDone() to upload-queue.js (lines 41-47) and awaits it after every readwrite request precisely because IDBRequest.onsuccess fires before the transaction commits. The hand-maintained sw.js writer of the share inbox has the same pre-fix pattern: storeIncomingShare resolves its promise on `req.onsuccess` (sw.js:145) and never listens for tx.oncomplete/onabort. Two concrete loss paths: (1) a transaction that aborts at commit time — e.g. QuotaExceededError while persisting a large shared video — fires after req.onsuccess, so the promise has already resolved; handleShareTarget's catch (sw.js:117-121) never runs, not even the console.warn, and the user is 303-redirected as if the share was stored while the record was rolled back. (2) handleShareTarget returns the redirect Response as soon as the add request succeeds; once respondWith settles the browser may terminate the service worker, racing the still-pending IDB commit. Either way the user's only copy of the shared photo/recording silently vanishes. This is distinct from known issues H7 (page-side runRequest(tx) hang in upload-queue/share-target-inbox remove), M22 (url-only shares), and M23 (rejection-path swallowing): those never examine the writer's commit handling.

**Fix:** Port txDone() into sw.js: after `req.onsuccess`, wait for `tx.oncomplete` before resolving, and reject on `tx.onabort`/`tx.onerror` so handleShareTarget's catch actually observes commit failures. Keep db.close() in finally (close() waits for pending transactions).

*Verification votes: real/high*

#### M3. bsk.2 fix gap: results.dropped is never surfaced — permanently dropped captures still vanish silently

`src/lab_tracker/frontend_src/shared/register-sw.js:71` · data-integrity · area: fix-collateral-frontend

Also independently reported as: Dropped captures are never surfaced to the user: every drain() caller discards results.dropped, so permanent and retry-limit drops are silent

Bead lab-tracker-bsk.2 (closed by c382cfe) explicitly required 'surface dropped items to the user', and the original H8 finding called out that every drain caller discards the results. The rewrite added a structured results.dropped array (with rejectedStatus/dropReason), but no production code reads it: grep shows '.dropped' consumed only inside upload-queue.js and its test file. All three drain call sites still discard the result — register-sw.js:71 and :75 `queue.drain().catch(() => {})`, mobile-capture.jsx:361 `queue.drain().catch(() => undefined)` — and the subscribe() notification carries no payload, so the PendingUploadsBadge can only show the count shrinking. When an item is deleted for a permanent 4xx or for retry_limit (including the stale-token 401 path above), the user gets no indication their capture was destroyed; the pending badge simply decrements as if it uploaded.

**Fix:** Have at least the mobile-capture and installOfflineRetry call sites inspect results.dropped and raise a flash/notification (e.g. 'N queued captures could not be uploaded and were removed'), or pass drop events through the subscriber channel so the badge component can surface them.

*Verification votes: real/high*

#### M4. bsk.1 (H7) fix incomplete: share-target-inbox.js remove() still awaits runRequest(tx), hanging share migration

`src/lab_tracker/frontend_src/shared/share-target-inbox.js:54` · bug · area: fix-collateral-frontend

Also independently reported as: share-target-inbox remove() still awaits IDBTransaction success event that never fires (path named in closed bead bsk.1)

Commit c382cfe added txDone() to upload-queue.js, but the identical never-settling pattern in share-target-inbox.js was left untouched even though the H7 finding and its fix prescription explicitly named it ('share-target-inbox.js remove() at line 54 has the same await runRequest(tx) bug'). IDBTransaction has no 'success' event, so the promise returned by runRequest(tx) never settles on the happy path. Consequence: migrateIncomingShares() enqueues the first OS share-sheet item, then hangs forever at 'await storage.remove(share.id)' (the delete itself auto-commits, only the JS promise hangs). The .then() in mobile-capture.jsx (lines ~351-361) never fires, so the 'shared capture queued — uploading now' flash never shows and the immediate queue.drain() never runs; with N parked shares, exactly one migrates per full page load. Bead lab-tracker-bsk.1 is CLOSED with 'Implemented P1 fixes and verified', so this is a fix-claims-resolved-but-does-not gap, not a tracked open issue.

**Fix:** Apply the same txDone() helper used in upload-queue.js to share-target-inbox.js remove() (and add an IndexedDB-shim test for this adapter, which would have caught the omission).

*Verification votes: real/high, real/high, real/high*

#### M5. drain() is not serialized: overlapping drains double-post items and double-count the new retry cap

`src/lab_tracker/frontend_src/shared/upload-queue.js:193` · bug · area: fix-collateral-frontend

drain() has no in-flight guard, and the app legitimately triggers overlapping invocations: installOfflineRetry drains at boot AND on every 'online' event (register-sw.js:70-76; rapid connectivity flaps fire several), and mobile-capture.jsx:361 drains again after share migration. Two concurrent drains both adapter.list() the same items and both POST each item, creating duplicate notes server-side before either remove() lands (remove of an already-deleted id succeeds silently, masking it). This was latent before, but the rewrite makes overlap strictly more costly in a new way: both drains call keepQueuedForRetry() for a failing item, so retryCount increments twice (or more) per real attempt window, burning the new MAX_RETRY_ATTEMPTS=5 budget at double speed and accelerating the silent retry_limit deletion. Additionally, when a concurrent drain has already removed the item, adapter.update() returns null and the `|| { ...item, ...patch }` fallback in keepQueuedForRetry (line 141) fabricates a phantom record that is pushed to results.stillQueued even though nothing remains in storage.

**Fix:** Serialize drain with a module-level in-flight promise (`if (draining) return draining; draining = doDrain().finally(() => { draining = null; });`), and treat a null adapter.update() result as 'item gone' (skip stillQueued) rather than fabricating a patched copy.

*Verification votes: real/high*

#### M6. New IndexedDB test uses a hand-rolled add-only fake; remove()/update() and the share-inbox adapter have no IDB coverage

`src/lab_tracker/frontend_src/shared/upload-queue.test.jsx:223` · testing · area: fix-collateral-frontend

The H7 fix prescription asked for a test 'against a fake-indexeddb shim so the real adapter is exercised'. The new test instead builds a minimal hand-rolled fake whose request.onsuccess fires via queueMicrotask and whose tx.oncomplete the test invokes manually — it verifies only that add() waits for oncomplete. It does not model transaction auto-commit, onerror/onabort, or event ordering, and it covers neither remove() nor the new multi-request update() path (get-then-put inside one transaction, the riskiest real-IDB pattern introduced by this commit). Critically, share-target-inbox.js's IndexedDB adapter has no IDB-backed test at all, which is exactly why its remaining `await runRequest(tx)` hang (finding 1) survived a close reason of 'verified with ... frontend tests'. The same bug class the P1 fixed is therefore still undetectable by CI in two of the three write paths.

**Fix:** Add fake-indexeddb (or equivalent shim) as a dev dependency and run the full createIndexedDbStorage and createIndexedDbShareStorage surfaces (add/list/remove/update) against it, including a hang-detection timeout assertion like the existing settled-flag pattern.

*Verification votes: real/high*

#### M7. FK enforcement now cascade-deletes graph change sets (including committed ones) when their source note is deleted; delete_note has no guard

`src/lab_tracker/services/note_service.py:327` · data-integrity · area: fix-collateral-backend

graph_change_sets.source_note_id is declared ON DELETE CASCADE (db_models.py:274-278, alembic/versions/0014_graph_draft_review.py:36), and graph_change_operations cascade from change sets. Before c382cfe this was a no-op on the default SQLite runtime; with PRAGMA foreign_keys=ON (db.py:35-43) a plain DELETE /notes/{id} now silently destroys every graph change set derived from that note — including COMMITTED change sets, which are the audit record of which graph operations were applied and why (the product's stated purpose is preserving this reasoning). delete_note performs no referential check before deleting. None of the closed beads (cq5.1-cq5.4) nor any open bead covers this newly-firing cascade; the prior review only catalogued the inverse orphaning failure. This is a behavior regression introduced by enabling the pragma without auditing which cascades it would activate.

**Fix:** Either block note deletion while committed change sets reference it (mirroring the new question/dataset/analysis guards), or change the FK/policy so committed change sets survive note deletion (e.g. nullable source_note_id with SET NULL plus retained snapshot fields). Add a test deleting a note with a committed change set.

*Verification votes: real/high*

#### M8. Project update/delete still gated on global WRITE_ROLES, locking out global-VIEWER project owners

`src/lab_tracker/services/project_service.py:87` · bug · area: gap:Project-scoped authorization overhaul (cq5.2 follow-on) — services and routes switched from global WRITE_ROLES to per-project require_contributor with entity-fetch-before-auth ordering; no authz findings exist for any of it

The authorization overhaul converted question/session/dataset/analysis/claim/visualization/note/goal writes to per-project ProjectAuthorizationPolicy checks, but ProjectService.update_project (line 87) and delete_project (line 102) were left on the old global gate require_role(actor, WRITE_ROLES) where WRITE_ROLES = {Role.ADMIN, Role.EDITOR} (services/shared.py:35). The corresponding routes, however, were switched to the per-project model: routes/projects.py:108 (PATCH) and :122 (DELETE) call ensure_project_owner(request, project_id). The two layers now contradict each other. All self-registered users default to global Role.VIEWER (schemas.py:140; only admins can mint EDITOR/ADMIN), and an admin-created project has NO owner membership row (project_service.py:54 skips it for admins), so the supported way to give a real user control of such a project is for the admin to grant them OWNER membership via upsert_project_membership. That granted owner (global VIEWER) passes ensure_project_owner at the route but then hits require_role(actor, {ADMIN, EDITOR}) inside the service and is rejected with AuthError 'Insufficient role.' The legitimate project owner therefore cannot rename, re-status, or delete their own project, even though they CAN manage its membership (upsert/delete_project_membership use require_owner). Conversely, at the service layer the global gate is also too permissive: any global EDITOR can update_project/delete_project for a project they have no membership in (mitigated for HTTP only because the route adds ensure_project_owner; internal/service callers are not).

**Fix:** Replace require_role(actor, WRITE_ROLES) in update_project/delete_project with self.authorization.require_owner(project_id, actor=actor) (resolving project_id is already the first argument), matching the routes and the rest of the converted surface, so project owners — regardless of global role — can manage their projects and non-member global editors cannot.

*Verification votes: real/high*

#### M9. Legacy malformed external_artifacts metadata now crashes every dataset read, not just provenance export

`src/lab_tracker/sqlalchemy_mappers.py:333` · bug · area: external-artifacts

Also independently reported as: dataset_from_model now parses legacy external_artifacts metadata on every read; malformed legacy values brick get/list datasets, extending known M33 beyond the provenance exporter

Bead c1u.7 (report M33) was a 500 on GET /datasets/{id}/provenance when manifest_metadata['external_artifacts'] is malformed. Commit e9342b9 fixed the write path (services/shared.py:374-378 wraps the decode in ValidationError -> 422) but moved the unguarded decode into the read mapper: dataset_from_model calls external_artifacts_from_metadata(metadata) with no error handling when the manifest_external_artifacts column is empty/NULL (which is true for ALL rows written before migration 0025). The old write path performed no validation on this metadata key (f71ea24 _normalize_commit_metadata accepts any string), so legacy rows with malformed values are possible. After upgrade, such a row raises raw json.JSONDecodeError (or pydantic ValidationError for shape mismatches) inside dataset_from_model, which is used by SQLAlchemyDatasetRepository.get/list/query (src/lab_tracker/sqlalchemy_repository_parts/datasets.py:47-75). Empirically reproduced end-to-end: both api.get_dataset and api.list_datasets raise JSONDecodeError. Neither exception type has a handler in routes/errors.py, so GET /datasets/{id} 500s, GET /datasets 500s for the whole collection (one bad row poisons the list), and because update_dataset/delete start with get_dataset, the row cannot even be repaired or deleted through the API. The docs updated in this same commit (docs/internal-boundaries.md) explicitly promise 'Older rows that encoded references in DatasetCommitManifest.metadata["external_artifacts"] remain readable as a legacy compatibility path.' The fallback at provenance.py:143 is similarly unguarded but is shadowed by the mapper in HTTP flows. Bead 61c2421 notes c1u.7 as 'possibly superseded' by e9342b9 — the supersession is partial: the blast radius for pre-existing bad rows widened from one endpoint to all dataset reads.

**Fix:** In dataset_from_model, wrap the legacy fallback in try/except ValueError and either drop unparseable legacy artifacts (logging a warning) or surface them via a degraded-but-readable representation, so legacy rows stay readable per the documented compatibility contract. Apply the same guard to the redundant fallback in provenance.py:_manifest_external_artifacts. Add a mapper test with a malformed legacy metadata value. Update bead c1u.7 to reflect partial supersession.

*Verification votes: real/high, real/high, real/high*

#### M10. import-folder exits 0 even when every file fails (server down, auth failure)

`src/lab_tracker_client/cli.py:185` · bug · area: evidence-inbox

_cmd_import_folder catches all per-file exceptions and records them in summary["errors"], which is the right call for continuing past one bad file, but nothing ever turns a non-empty errors list into a non-zero exit status. main() (cli.py:20-29) just prints the JSON summary and returns None, so the process exits 0. If the API is unreachable, auth is misconfigured, or the project UUID is wrong, every single file lands in errors and the command still reports success to the shell. README and docs explicitly position this adapter for scheduled/synced-folder automation ("Scheduled graph-draft batches can later propose..."), where cron/CI detect failure via exit codes — a fully failed inbox sweep is indistinguishable from a clean one.

**Fix:** After the loop, exit non-zero when summary["errors"] is non-empty (e.g. print the summary then `raise SystemExit(1)`), optionally behind a --strict/--no-strict flag. Also consider failing fast when the first N consecutive files fail with the same connection/auth error instead of retrying the whole folder.

*Verification votes: real/high*

#### M11. evidence_content_hash is computed from a separate read than the uploaded bytes, so a file changing mid-import records a wrong provenance hash

`src/lab_tracker_client/client.py:678` · data-integrity · area: evidence-inbox

Also independently reported as: import_evidence_file hashes the file in one pass and uploads it in a second read; on actively syncing folders the recorded evidence_content_hash can mismatch the stored bytes

import_evidence_file hashes the file (`content_hash = file_sha256(path)`, client.py:678), runs the duplicate scan over the network, and only then calls upload_note_file, which re-reads the file from disk (`payload = path.read_bytes()`, client.py:627). The adapter's stated target environment is folders actively written by sync clients (Google Drive/Dropbox/OneDrive per README and docs/evidence-source-metadata.md), where partially-synced files change between the two reads. When that happens, the stored note's evidence_content_hash does not match the bytes actually uploaded — docs/evidence-source-metadata.md defines the key as "SHA-256 hash of the imported evidence bytes". Downstream effects: the dedupe contract is silently broken for that item (the next run hashes the settled file, finds no match, and imports again, leaving a phantom "version" whose recorded hash matches nothing), and provenance verification against the stored asset fails.

**Fix:** Read the bytes exactly once in import_evidence_file, hash those bytes (hashlib.sha256(payload)), and pass the same bytes to the upload (add a content/bytes parameter to upload_note_file or build the multipart there). This also halves disk I/O per file.

*Verification votes: real/high*

#### M12. Known fetch-all defect (M68/c1u.8) propagated into find_evidence_note and amplified to O(files x notes) by the per-file import loop

`src/lab_tracker_client/client.py:532` · performance · area: evidence-inbox

The new find_evidence_note inherits the tracked M68 behavior (list_* always paginates the entire collection) and import-folder calls it once per candidate file (client.py:691-696 inside import_evidence_file, invoked per file from cli.py:171-184). A folder of M files in a project with N notes costs M x ceil(N/200) GET /notes requests, and the server route makes each of those requests O(N) itself (routes/notes.py:163-170 fetches with `limit=None, offset=0` and paginates in memory). The steady state of an evidence inbox is re-running over a mostly-imported folder where nearly every file is a dedupe skip, so every scheduled run pays the full quadratic cost, and N grows with each import. --dry-run pays the same network cost. While the root limit-semantics defect is already tracked, building a bulk per-file scan on top of it in new code is a new, materially worse cost profile.

**Fix:** Fetch the project's notes once per import-folder run and build an in-memory index keyed by (evidence_source_provider, evidence_source_external_id, evidence_content_hash); pass it into import_evidence_file (or add a batch import API on LabTracker). Longer term, add server-side metadata filtering so dedupe is a single query.

*Verification votes: real/high*

#### M13. import-folder dedupe is O(files x notes): find_evidence_note re-pages the entire project note list once per imported file

`src/lab_tracker_client/client.py:685` · performance · area: diff-sweep

`import_evidence_file` calls `self.find_evidence_note(...)` for every candidate file, and find_evidence_note (client.py:521-540) iterates `self.list_notes(project_id=...)`, which pages the FULL project note list (200/page) from the API on each call. The CLI loop in cli.py:_cmd_import_folder therefore issues files x ceil(notes/200) GET /notes requests per run. The README advertises this for scheduled imports of synced folders, where each run adds notes to the project, so traffic grows quadratically over time: a 1,000-file inbox in a project that has accumulated 5,000 notes is 1,000 x 25 = 25,000 HTTP requests for a run that imports nothing new. There is also no server-side filter on the evidence metadata, so the work cannot be pushed down.

**Fix:** Fetch the project's notes once per CLI run, build an in-memory index keyed by (evidence_source_provider, evidence_source_external_id, evidence_content_hash), and pass it into the per-file import; or add a server-side metadata filter to GET /notes.

*Verification votes: real/high*

#### M14. Test-helper SQLite engines omit PRAGMA foreign_keys=ON, so cascade-dependent guards are untestable and the cq5.4 guard is bypassable in the test environment

`tests/api_helpers.py:17` · testing · area: fix:lab-tracker-cq5.4

Also independently reported as: Service/repository test engines bypass SQLite FK pragmas, so most of the suite still runs with FK enforcement off

Bead lab-tracker-cq5.1 enabled PRAGMA foreign_keys=ON in the production engine (src/lab_tracker/db.py:35-44), but tests/api_helpers.py repository_backed_api() builds its own engine without the pragma, as do test_auth_routes.py:22, test_device_auth.py:27, test_persistence_flows.py, test_observability.py, test_service_composition.py:46 and test_sqlalchemy_repository.py:45. With FK enforcement off, ON DELETE CASCADE on claim_analyses never fires in tests. I verified this materially diverges from production: running the new delete_analysis guard scenarios against the api_helpers-style engine, (a) deleting an analysis that supports a PROPOSED claim leaves a dangling claim_analyses row (the claim still reports the deleted analysis in supported_by_analysis_ids), and (b) for a SUPPORTED claim backed by analyses A and B, deleting A then B succeeds — the dangling A row counts as 'remaining support' in _ensure_analysis_can_be_deleted, so the SUPPORTED claim silently loses its last real support, the exact bug lab-tracker-cq5.4 was meant to prevent. The same scenarios pass with the pragma enabled. The new regression test in tests/test_analysis_claims_visualizations.py therefore runs under DB semantics no supported runtime uses, and the suite is structurally unable to catch cascade-dependent regressions in any delete guard.

**Fix:** Have tests/api_helpers.py (and the other test engine factories) reuse lab_tracker.db.get_engine or attach the same _configure_sqlite_engine connect listener so test SQLite engines enforce foreign keys, then add a guard test covering the sequential-delete case (claim supported by two analyses: deleting the first succeeds, deleting the survivor is rejected).

*Verification votes: real/high*

#### M15. Packaging guard test cannot detect the subdirectory failure mode it exists to prevent

`tests/test_packaging.py:18` · testing · area: fix:lab-tracker-f6k.1

The packaging guard test added in c382cfe does not build or inspect the real wheel (as the original finding's expected fix prescribed); it re-implements pattern matching with fnmatch against the pyproject patterns. This simulation diverges from setuptools' actual glob semantics in exactly the scenario the glob fails: subdirectories. (1) tests/test_packaging.py:18-19 enumerates bundle files with frontend_dir.iterdir() filtered by is_file(), which never recurses, so any file in a future frontend/<subdir>/ is invisible to the test. (2) tests/test_packaging.py:24 uses fnmatch, whose '*' crosses '/' (fnmatch.fnmatch('frontend/assets/x.js', 'frontend/*') returns True), whereas setuptools' glob-based package-data matching for 'frontend/*' matches only the directory entry and ships none of its contents (verified empirically with glob.glob). Consequence: if the frontend bundle ever gains a subdirectory (e.g. esbuild outdir with code splitting, asset/font dirs, sourcemaps in a subfolder), those files are silently excluded from the wheel while the guard test stays green — recreating the exact original 404/500 bug class undetected. Latent today because scripts/build-frontend.mjs emits a single flat outfile (app.js), so the bundle dir is flat.

**Fix:** Make the test build the wheel (e.g. via 'python -m build --wheel' into a tmpdir, or setuptools' build_py data-file resolution) and assert the wheel's lab_tracker/frontend/* entries equal the recursive contents of src/lab_tracker/frontend (rglob). Alternatively, at minimum use Path.rglob in the test and replace fnmatch with PurePosixPath.match or glob-equivalent semantics, and add 'frontend/**/*' (or per-subdir globs) to package-data so subdirectory files are shipped.

*Verification votes: real/high*

### LOW (4)

#### L1. Analysis delete guard over-blocks on REJECTED claims, making such analyses permanently undeletable

`src/lab_tracker/services/analysis_service.py:244` · bug · area: fix-collateral-backend

_ensure_analysis_can_be_deleted (new in c382cfe for lab-tracker-cq5.4) skips only PROPOSED claims and blocks deletion when the analysis is the last support link 'for a non-proposed claim'. But the support invariant it protects applies only to SUPPORTED claims (_ensure_claim_support_links, services/shared.py:284-285: only `status == ClaimStatus.SUPPORTED` requires support). REJECTED claims are allowed to have zero support, yet they still block analysis deletion — and there is no escape: REJECTED is a terminal status (shared.py:157 `ClaimStatus.REJECTED: {ClaimStatus.REJECTED}`) and non-proposed claims cannot have their support links edited (claim_service.py:134-141 'Only proposed claims can be edited.'). So an analysis whose only remaining reference is a rejected claim's support link can never be deleted unless the user destroys the rejected claim record itself — exactly the provenance the rejection was meant to preserve. The new test only covers the SUPPORTED case (tests/test_analysis_claims_visualizations.py:215-246).

**Fix:** Either restrict the guard to ClaimStatus.SUPPORTED, or (if rejected-claim evidence must also be preserved) allow editing/clearing support links on rejected claims so the dead-end is escapable. Add a test for the REJECTED-claim case documenting the chosen policy.

*Verification votes: real/high*

#### L2. Commit guard asymmetry: legacy metadata-encoded artifacts satisfy create-committed but are rejected by update-commit

`src/lab_tracker/services/dataset_service.py:220` · bug · area: external-artifacts

create_dataset checks the empty-commit guard AFTER _build_commit_manifest has merged legacy metadata-encoded artifacts into the resolved manifest (dataset_service.py:115-127 checks resolved_manifest.external_artifacts), but the update->commit path checks the RAW input before the merge (line 220: 'if not files and not base_manifest.external_artifacts'). The legacy metadata["external_artifacts"] encoding is still a documented supported write shape (docs/build-vs-buy-boundaries.md calls it a 'legacy read/export fallback' and _build_commit_manifest deliberately merges it). Result: the identical file-less commit_manifest carrying only legacy-encoded artifacts is accepted by POST /datasets with status=committed but rejected with 'At least one file or external artifact is required to commit a dataset.' when supplied to PATCH/update with status=committed. Empirically reproduced: create(committed) with legacy-encoded artifact only succeeded (DatasetStatus.COMMITTED); update->committed with the same manifest raised ValidationError.

**Fix:** Move the update-path guard after _build_commit_manifest (check resolved_manifest.files/external_artifacts like create_dataset does), or run _legacy_external_artifacts(base_manifest.metadata) in the guard so both commit paths treat the legacy encoding identically.

*Verification votes: real/high*

#### L3. Staged datasets with duplicate legacy external_artifacts metadata can no longer be committed or updated, and silent dedup flips the commit hash

`src/lab_tracker/services/shared.py:390` · data-integrity · area: gap:External-artifact manifest core: commit-hash payload conditional, merge/dedup keyed without content_hash, provenance ingestion roundtrip, migration 0025 — only the mapper/guard edges were reviewed

Pre-upgrade, metadata["external_artifacts"] was opaque user-supplied data: the commit path never decoded it (verified: `git show f71ea24:src/lab_tracker/services/shared.py` contains no occurrence of 'external'), and encode_external_artifacts never deduplicated, so rows whose encoded list contains the same (kind, source_system, uri) twice (exact duplicates, or the same URI pinned at two content_hashes) can legitimately exist. Post-upgrade, two failure modes hit such rows. (1) Update lockout: the mapper backfills manifest.external_artifacts from the metadata key without dedup (sqlalchemy_mappers.py:330-333), _manifest_input_from_commit (shared.py:418-427) feeds that backfilled list into the EXPLICIT slot on any update with commit_manifest=None, and _build_commit_manifest -> _normalize_external_artifacts raises. Empirically verified: building a manifest whose external_artifacts carry the mapper-backfilled duplicate list raises ValidationError 'Duplicate external artifact reference in commit manifest.' So a staged pre-upgrade dataset with a duplicated legacy entry can never be committed, have question links changed, or be promoted — every manifest-refreshing update_dataset call 400s (only status-only updates still work). Same-URI-different-hash legacy pairs fail identically ('Duplicate' via the explicit slot, or 'Conflicting external artifact reference' via _merge_external_artifacts line 407 when resubmitted metadata-only — both verified). (2) Silent hash flip: if the client instead resubmits the manifest metadata-only (explicit list empty), _merge_external_artifacts silently drops the exact duplicate (the `continue` at line 408), making the merged list [X] differ from the legacy decode [X, X]; the conditional at line 533 then flips from omitting to INCLUDING the 'external_artifacts' key in the hashed payload. Verified: same files, same metadata, recomputed hash differs from the hash the identical content produced before the rebuild (payload gains the key, artifact count drops 2 -> 1). The content-addressed hash of unchanged content thus depends on which code path last touched the manifest.

**Fix:** Make the legacy decode path tolerant: dedup exact duplicates during mapper backfill and in _legacy_external_artifacts (so the legacy list and the merged list agree and the line-533 conditional cannot flip), and route mapper-backfilled artifacts through the legacy-tolerant merge rather than the strict _normalize_external_artifacts explicit path (e.g. track which artifacts came from metadata in _manifest_input_from_commit, or dedup-with-tolerance instead of raising when the duplicates are byte-identical).

*Verification votes: real/high*

#### L4. Default dedupe key (provider='local-folder' + root-relative path) collides across different --root folders, silently skipping or falsely versioning distinct files

`src/lab_tracker_client/cli.py:178` · data-integrity · area: evidence-inbox

The CLI sets `source_external_id=relative_path` (root-relative POSIX path) with a fixed default provider "local-folder" (cli.py:76-79). The dedupe key is (project, provider, external_id, content_hash) with no trace of the root, so importing two different folders into the same project shares one ID namespace. Concretely: (1) files at the same relative path with identical content in two roots — the second folder's file is reported "skipped: duplicate" and its source URI is never recorded; (2) same relative path, different content — both notes are created sharing the same external_id, which per docs/evidence-source-metadata.md ("Changed file contents therefore create a new staged evidence note") reads as version history of a single item that never existed. Template-named files (README.md, notes.md, instrument export names) make this realistic for anyone running the command against more than one inbox. Additionally, build_evidence_metadata strips the external_id via _require_non_empty (client.py:898-901, `str(value).strip()`), so a filename with leading/trailing whitespace is stored under a mutated ID that no longer round-trips to the real relative path and collides with its trimmed sibling.

**Fix:** Namespace the default external_id by the root (e.g. the root's file URI + '/' + relative path, or derive a per-root provider suffix), or at minimum document loudly that distinct roots imported into one project must use distinct --provider values. Stop stripping external_id in build_evidence_metadata (validate non-empty without mutating).

*Verification votes: real/high*

## Low-severity findings (single reviewer, unverified)

- **Empty files: dry-run reports them as importable but real runs error on every scheduled sweep forever** — `src/lab_tracker_client/client.py:631` (bug, evidence-inbox)
  - import_evidence_file does not check for empty files; upload_note_file rejects them (`raise LTValidationError("file_path must point to a non-empty file.")`, client.py:631) only after the hash and the full dedupe scan. Consequences: (1) --dry-run returns action=skipped/reason=dry_run for a 0-byte file (client.py:708-717), claiming it would import, while the real run records an error for it; (2) since an empty file can never be imported, it reappears in summary["errors"] on every subsequent run — and sync clients (the adapter's target) routinely create 0-byte placeholder files before content hydrates, as do .gitkeep-style markers, producing permanent error noise that masks real failures (worsened by the exit-code finding above).
  - Fix: Check the file size up front in import_evidence_file (or in _discover_import_files) and return a deterministic skipped result (e.g. reason="empty") for 0-byte files in both dry-run and real runs, instead of erroring.
- **Upload path buffers the entire file in memory, unlike the streaming hash next to it** — `src/lab_tracker_client/client.py:627` (performance, evidence-inbox)
  - upload_note_file reads the whole file with `path.read_bytes()` and hands the bytes to httpx multipart, so a large binary evidence file (microscopy stacks, videos, raw datasets — the import-folder default include is '*', sweeping everything in the inbox) is held fully in client memory per upload. The sibling file_sha256 helper (client.py:859-868) was deliberately written to stream in 1 MiB chunks, so within one import_evidence_file call the same file is streamed once and then fully buffered anyway. There is also no client-side size guard or skip threshold.
  - Fix: Pass an open binary file object to httpx's files= (it streams file objects) or, if fixing the hash/upload consistency finding by reading once, document/limit a maximum import size and skip larger files with an explicit skipped reason.
- **Same URI accepted as both entity and activity artifact, producing one JSON-LD node with disjoint PROV types** — `src/lab_tracker/services/shared.py:414` (data-integrity, external-artifacts)
  - _external_artifact_key includes kind.value, so the duplicate/conflict validation in _normalize_external_artifacts/_merge_external_artifacts permits two artifacts that share source_system and uri but differ in kind (one ENTITY, one ACTIVITY). The exporter then emits two @graph nodes with the identical @id (artifact.uri is used directly as @id, provenance.py:147-151), one typed prov:Entity and one prov:Activity, and the commit activity references that same @id from both prov:used and prov:wasInformedBy. Under JSON-LD flattening these merge into a single node typed both prov:Entity and prov:Activity — disjoint classes under PROV constraints (typing) — so any consumer doing real PROV-O processing gets an invalid graph. The write path explicitly enables this state rather than rejecting it.
  - Fix: Drop kind from _external_artifact_key (or add a separate uniqueness check on (source_system, uri)) so the same URI cannot be committed under both kinds, raising the existing 'Conflicting external artifact reference' ValidationError.
- **Artifact uri is emitted as JSON-LD @id but validated only as a non-blank string** — `src/lab_tracker/models.py:287` (api-design, external-artifacts)
  - ExternalArtifactReference._require_nonblank only strips and rejects blank values for uri, yet the provenance exporter uses uri verbatim as the node @id (provenance.py:148) and as the @id-typed externalUri value (context: {"@id": "lab:externalUri", "@type": "@id"}). A uri like 'my run notes' or 'C:\\data\\run1' commits fine but is not an IRI, so the exported document fails or mangles under JSON-LD expansion (relative-IRI resolution, illegal characters), undermining the PROV-O export the feature exists for. The same lax validation existed in the old metadata-encoded seam, but e9342b9 promotes uri to a first-class, directly-settable API field on DatasetCommitManifestInput (exposed via POST /datasets per test_dataset_route_accepts_external_artifact_manifest_without_files), substantially widening reachability. Not covered by the previous review (M33/viz-asset context findings are distinct).
  - Fix: Validate uri as an absolute IRI at write time (e.g. require a scheme via urllib.parse.urlsplit and reject characters illegal in IRIs), or percent-encode/skolemize non-IRI uris when building the @id in the exporter.
- **busy_timeout is set after journal_mode=WAL, so the one-time WAL conversion can fail with 'database is locked' under concurrent first connections** — `src/lab_tracker/db.py:37` (bug, fix-collateral-backend)
  - The connect-event listener added in c382cfe executes the pragmas in the order foreign_keys, journal_mode=WAL, busy_timeout=5000. Converting an existing DELETE-journal database file to WAL requires an exclusive lock, and at that point the connection's busy timeout is still the pysqlite default (the driver-level timeout applies to connect, not to this pragma's lock acquisition window in all cases). On first startup against a pre-existing (pre-fix) database with multiple workers/threads connecting simultaneously — e.g. uvicorn's thread pool serving two requests, each opening a pooled connection — the journal_mode=WAL statement can raise OperationalError('database is locked') and fail the request at connection time. Once converted, WAL is sticky and the race disappears, which makes this an intermittent first-boot failure that is hard to diagnose.
  - Fix: Execute `PRAGMA busy_timeout=5000` first, before `PRAGMA journal_mode=WAL`, so the WAL conversion (and any FK pragma contention) benefits from the retry window.
- **Mutation endpoints now fetch the entity before authorization, creating a cross-project existence oracle (404 vs 401) for non-members** — `src/lab_tracker/services/analysis_service.py:159` (security, fix-collateral-backend)
  - c382cfe moved authorization from a global role gate executed before any lookup (`require_role(actor, WRITE_ROLES)`) to a project-scoped check executed after the entity fetch in every update/delete path: update_analysis (analysis_service.py:159-160), delete_analysis (179-180), update_claim (claim_service.py:130-131), delete_claim (171-172), update_dataset (dataset_service.py:173-174), delete_dataset (261-262), update/delete_question, update/delete_session, and the visualization paths. An authenticated user with no membership in any project can now distinguish existing from non-existing UUIDs on PATCH/DELETE: NotFoundError (404) means the id does not exist, AuthError (401) means it exists in a project they cannot access. Mitigating factors: ids are random UUIDv4s, and the GET routes already had the same get-then-ensure_project_read ordering before this commit, so little net-new information is exposed — but the fix standardized the leaky ordering on every write path instead of fixing it.
  - Fix: Return 404 for entities in projects the actor cannot read (catch AuthError from the read check and re-raise NotFoundError), so missing and forbidden resources are indistinguishable, matching common REST authorization practice.
- **New delete guards are check-then-act: on Postgres a concurrently added support link is silently cascaded away between guard and delete** — `src/lab_tracker/services/analysis_service.py:179` (data-integrity, fix-collateral-backend)
  - All three new guards (_ensure_analysis_can_be_deleted at analysis_service.py:244, _ensure_dataset_can_be_deleted at dataset_service.py:268, _ensure_question_not_primary_reference at question_service.py:386) run a SELECT in the request transaction and then perform the delete in a separate unit_of_work within the same transaction. Under Postgres READ COMMITTED (the documented multi-client runtime), a claim that gains its sole support link from the analysis between the guard's read and the delete's commit is not seen by the guard, and the ON DELETE CASCADE on claim_analyses then silently strips the freshly committed claim's last support — exactly the corruption lab-tracker-cq5.4 was meant to prevent, surviving in a race window. On the default SQLite runtime the single-writer model plus snapshot-stale SQLITE_BUSY largely prevents the interleaving, so this is Postgres-specific and low likelihood, but the prior review's closed P1 finding is only probabilistically fixed there.
  - Fix: On Postgres, take a SELECT ... FOR UPDATE on the analysis row (or use SERIALIZABLE for delete endpoints), or re-validate inside the same statement via a NOT EXISTS-guarded DELETE; alternatively change claim_analyses to ON DELETE RESTRICT so the database enforces the invariant race-free.
- **import-folder follows file symlinks outside --root and uploads the target's contents as evidence** — `src/lab_tracker_client/cli.py:227` (security, diff-sweep)
  - `_discover_import_files` enumerates `root.rglob("*")` filtered by `path.is_file()`, and `is_file()` follows symlinks. A symlink placed inside the inbox folder (e.g. extracted from an archive someone shared into the synced folder, or planted by another local user) that points at a file outside --root (e.g. ~/.ssh/id_ed25519, a .env file) is treated as a regular inbox file: its target's bytes are hashed and uploaded to the Lab Tracker server as a staged evidence note under an innocuous relative name. The include/exclude globs match the symlink's name, not the target, so they provide no protection, and nothing in the summary indicates the file was a link.
  - Fix: Skip symlinks during discovery (`if path.is_file() and not path.is_symlink()`), or at minimum resolve each candidate and require `resolved.is_relative_to(root)` before reading, recording skipped links in the summary.
- **README import-folder quick-start expands $LAB_TRACKER_PROJECT_ID before the same-command env assignment applies, so --project is empty** — `README.md:216` (docs, diff-sweep)
  - The new README example sets LAB_TRACKER_PROJECT_ID as a command-prefix assignment and references "$LAB_TRACKER_PROJECT_ID" in the same command line. In POSIX shells, parameter expansion happens when the command line is parsed, BEFORE the temporary assignments are placed in the command's environment, so `--project "$LAB_TRACKER_PROJECT_ID"` expands to an empty string unless the variable was already exported in the shell. The CLI's required `--project` then receives "", and the import fails (or every file lands in the errors list) — the documented quick-start for the new feature does not work as written.
  - Fix: Either use `export LAB_TRACKER_PROJECT_ID=<project-id>` on its own line first, or drop the variable and write `lt import-folder --project <project-id> --root ...`. (Note the client also reads LAB_TRACKER_PROJECT_ID as default_project_id, but the CLI requires the explicit --project flag.)
- **Dataset delete reference checks are check-then-act; DB FKs still CASCADE so a concurrent claim/analysis link can be silently dropped** — `src/lab_tracker/services/dataset_service.py:268` (data-integrity, fix:lab-tracker-cq5.3)
  - _ensure_dataset_can_be_deleted queries claims/analyses in the request session before delete_dataset commits the delete in a separate unit_of_work. The dataset_id foreign keys on claim_datasets (db_models.py:562-566) and analysis_datasets (db_models.py:523-527) still declare ondelete="CASCADE", so the invariant is enforced only at the application layer. Under concurrent requests (Postgres READ COMMITTED), a claim or analysis linked to the dataset after the check but before the delete commits would still have its join rows cascaded away, silently losing evidence -- the exact failure mode of the original finding, now narrowed to a race window.
  - Fix: Change the dataset_id FKs on claim_datasets and analysis_datasets to ondelete="RESTRICT" (with an Alembic migration) so the database itself rejects deleting a referenced dataset, making the service-level ValidationError the friendly path and the FK the hard guarantee.
- **Deny-path regression tests missing for most fee.1 write gates** — `tests/test_project_collaboration.py:122` (testing, fix:lab-tracker-fee.1)
  - The fix's only denial assertion is a project viewer being rejected on POST /questions (tests/test_project_collaboration.py:142-151). None of the other 14 gated mutating paths have a denial test: question PATCH/refactor/DELETE, session PATCH/DELETE, session outputs POST, promote, promote-to-dataset, dataset PATCH/DELETE, dataset file upload/delete, nor the cross-project case (global EDITOR with no membership) that was the core of the original IDOR finding. A future refactor that drops one require_contributor call (e.g. during a service split) would pass the suite. I verified all paths are currently denied via a live probe, so this is a coverage gap, not a defect. Open bead lab-tracker-aai.2 only covers list-endpoint read scoping, so this is not already tracked.
  - Fix: Parametrize a denial test over the full mutation matrix (method, url) for two actors: a project viewer member and a global EDITOR non-member, asserting 401 and that the record is unchanged; mirror the probe matrix used in this verification.
- **get_* runs before require_contributor, yielding a cross-project existence oracle (404 vs 401)** — `src/lab_tracker/services/claim_service.py:130` (security, gap:Project-scoped authorization overhaul (cq5.2 follow-on) — services and routes switched from global WRITE_ROLES to per-project require_contributor with entity-fetch-before-auth ordering; no authz findings exist for any of it)
  - In the converted update/delete methods the entity is fetched before the authorization check fires: claim_service.update_claim (line 130 get_claim, line 131 require_contributor), delete_claim (171/172), session_service.delete_acquisition_output (225 get output then 231 require_contributor), visualization_service.update/delete (100-102, 129-131), analysis_service (159-160, 179-180), etc. The same ordering exists at the HTTP layer, which fetches the entity to learn its project_id before calling ensure_project_read (e.g. routes/claims.py:83-84). No entity body is ever returned before the AuthError is raised, so this is not a data-disclosure bug, but it does let an unauthorized caller distinguish 'entity exists in a project you can't see' (AuthError -> 401) from 'entity does not exist' (NotFoundError -> 404), i.e. an existence/ID-enumeration oracle across project boundaries. This is consistent with the app-wide 404/401 pattern, so severity is low.
  - Fix: Accept as a known low-risk tradeoff, or normalize unauthorized-vs-missing responses for cross-project access (return 404 when the actor has no read access to the entity's project) so existence cannot be probed; if changed, do it uniformly across the get-before-auth methods rather than per-call.
- **dataset_manifest_from_external_artifact silently changed its output encoding, so identical adapter input now yields a different commit_hash than pre-upgrade and caller-supplied legacy keys are no longer overwritten** — `src/lab_tracker/provenance_ingestion.py:59` (api-design, gap:External-artifact manifest core: commit-hash payload conditional, merge/dedup keyed without content_hash, provenance ingestion roundtrip, migration 0025 — only the mapper/guard edges were reviewed)
  - This helper is the documented adapter seam (exported in __all__, described in docs/internal-boundaries.md). Pre-change it wrote `merged_metadata[EXTERNAL_ARTIFACTS_METADATA_KEY] = encode_external_artifacts([artifact])` into manifest metadata; post-change it returns `external_artifacts=[artifact]` and leaves metadata untouched. Because _manifest_payload hashes the two encodings differently (legacy: artifacts live inside the hashed metadata string with no 'external_artifacts' payload key; first-class: metadata lacks the string and the payload gains the key, per shared.py:533), the same adapter call for the same external artifact + files now produces a different commit_hash than the dataset it created before the upgrade. Any adapter using commit_hash for idempotent re-ingestion ('has this external manifest already been ingested?') will see a miss and create a duplicate dataset for already-ingested content. Additionally, the old behavior unconditionally OVERWROTE a caller-passed metadata['external_artifacts'] key; the new behavior preserves it, so an adapter that carries forward previous manifest metadata containing the old pin and passes an updated artifact (same URI, new content_hash) now gets a 400 'Conflicting external artifact reference' at commit (verified via the merge path) where the old code succeeded cleanly.
  - Fix: Document the hash discontinuity for adapter authors (internal-boundaries.md mentions readability of old rows but not that re-ingestion hashes change), and have the helper strip or supersede a caller-supplied legacy 'external_artifacts' metadata key (matching its old overwrite semantics) so carried-forward metadata cannot collide with the first-class artifact.
- **No test pins the central backward-compat property: legacy metadata-encoded manifests must recompute to their pre-upgrade commit_hash** — `tests/test_core_api.py:115` (testing, gap:External-artifact manifest core: commit-hash payload conditional, merge/dedup keyed without content_hash, provenance ingestion roundtrip, migration 0025 — only the mapper/guard edges were reviewed)
  - The conditional at shared.py:533 exists solely so pre-upgrade datasets (artifacts encoded only in metadata['external_artifacts']) keep re-hashing to their stored commit_hash, but nothing tests it. The added coverage tests order-insensitivity and content-sensitivity of first-class artifacts (test_external_artifacts_are_content_addressed_deterministically), commit-without-files, explicit duplicate rejection, mapper legacy backfill (test_dataset_mapper_reads_legacy_external_artifact_metadata), and legacy provenance export — but there is no test that (1) a manifest whose artifacts live only in metadata hashes to the same value the pre-feature code produced (i.e. that 'external_artifacts' stays out of the hashed payload when merged == legacy — the property I had to verify by hand-running _manifest_payload); (2) a legacy dataset read from the DB can still be committed/updated (the path that empirically raises 'Duplicate external artifact reference' for dirty legacy rows, finding 1); (3) the _merge_external_artifacts 'Conflicting' path between metadata-encoded and first-class inputs behaves as intended (no test mentions 'Conflicting' anywhere in tests/); (4) a full DB round-trip of a mixed manifest (legacy + new artifact) re-validates its stored hash. Without (1), any future refactor of _manifest_payload can silently break every pre-upgrade dataset's stored hash and no test will fail.
  - Fix: Add a regression test that builds a manifest with metadata={'external_artifacts': encode_external_artifacts([X])}, asserts _manifest_payload omits the 'external_artifacts' key and that the hash equals a hard-coded pre-upgrade value; add tests for commit/update of a repository-round-tripped legacy dataset and for the legacy-vs-explicit Conflicting/Duplicate merge paths.
- **Writer/reader contract: sw.js stores filename and contentType fields the migration reader never consumes; Blob-typed shares upload as 'capture' instead of the recorded 'shared' name** — `src/lab_tracker/frontend/sw.js:109` (api-design, gap:Service-worker writer side of the share pipeline (hand-maintained sw.js unchanged while its reader was fixed), committed-bundle/source consistency, and the useAuthSession 401/403 fix verdict)
  - The DB name ('lab-tracker-share-inbox'), version (1), store ('pending'), and keyPath ('id', autoIncrement) match exactly between writer (sw.js:25-26, 127, 131) and reader (share-target-inbox.js:13-15, 19, 23), so no VersionError/blocked-upgrade risk — that part is clean. However the writer persists `filename: file.name || "shared"` and `contentType: file.type || "application/octet-stream"` (sw.js:109-110) and the reader never reads either: migrateIncomingShares passes only `file: share.file` to enqueue (share-target-inbox.js:116-121), and enqueue re-derives `filename: file.name || "capture"` (upload-queue.js:174). For File objects the name survives structured clone so the result is the same, but sw.js:106 explicitly accepts bare Blobs (`file instanceof File || file instanceof Blob`), and a Blob has no .name — the stored 'shared' filename is discarded and the multipart upload posts the part as 'capture' with the Blob's own type. The two stored fields are dead weight at best and a silently inconsistent contract at worst.
  - Fix: Either have migrateIncomingShares forward share.filename into enqueue (add a filename override parameter to enqueue) so the writer-recorded name is honored, or stop storing filename/contentType in the inbox record to keep the contract honest.

## Open-bead recheck

Every open bead from the 2026-06-12 review was checked against the new code. Statuses:

- **lab-tracker-cq5.5**: open
  - No entity delete path cleans up goal links: grep for query_goal_links/goal_link across src/lab_tracker/services shows calls only inside goal_service.py itself (link CRUD) and graph draft code; delete_dataset (services/dataset_service.py:260-290), delete_analysis (services/analysis_service.py:178-184), delete_claim (services/claim_service.py:170-175) check claims/analyses but never goal links. goal_service.py unchanged in f71ea24..HEAD.
- **lab-tracker-cq5.6**: open
  - src/lab_tracker/services/project_service.py unchanged in diff. upsert_project_membership (lines 141-166) still demotes any membership role with no owner-count check; delete_project_membership (lines 179-185) still does an unlocked check-then-act owner count (no with_for_update / guarded DELETE).
- **lab-tracker-cq5.7**: open
  - Only new migration is alembic/versions/0025_dataset_external_artifacts.py; no tightening migration added. 0025 actually extends the drift: it adds manifest_external_artifacts nullable=True (0025:17) while db_models.py:150-153 declares it as a non-Optional Mapped[list] (NOT NULL in metadata-built schemas).
- **lab-tracker-cq5.8**: open
  - alembic/versions/0023_goal_link_slot_not_null.py is not in the f71ea24..HEAD file list and no follow-up migration dedupes unslotted goal links; the only new migration (0025) touches the datasets table only.
- **lab-tracker-cq5.9**: open
  - No migration drops the 0017_daily_graph_reviews tables or renames ix_graph_change_sets_batch_key to match the model's uq_graph_change_sets_batch_key; alembic/versions contains only the unrelated 0025_dataset_external_artifacts.py as new head.
- **lab-tracker-cq5.10**: open
  - src/lab_tracker/services/dataset_service.py:219 still reads `files = attached_files or list(base_manifest.files)` in the commit-refresh path (line moved from 211 to 219; expression identical) — manifest-declared files are still silently discarded whenever any uploaded file is attached.
- **lab-tracker-fee.3**: open
  - src/lab_tracker/routes/projects.py unchanged: PATCH /projects/{id}/members/{user_id} (lines 215-232) still calls upsert_project_membership with no user-existence or membership-existence check, and project_service.py:151-159 still creates a new membership when none exists, returning 200 instead of 404.
- **lab-tracker-fee.4**: open
  - src/lab_tracker/routes/projects.py:206 still calls _resolve_member_user_id (which raises 404 NotFoundError for unknown users, projects.py:250-255) before the owner check that lives inside upsert_project_membership (project_service.py:149), so the user-enumeration ordering is unchanged.
- **lab-tracker-fee.5**: open
  - src/lab_tracker/auth.py unchanged in diff: consume_enrollment (auth.py:417-447) still does a plain select (no with_for_update), checks consumed_at at line 433, and only sets enrollment.consumed_at at line 447 — no conditional UPDATE, so concurrent consumption still mints multiple device tokens.
- **lab-tracker-zl9.4**: open
  - src/lab_tracker/routes/visualizations.py download still serves `inline` disposition (line 191) with the client-supplied content type (line 197, media_type=row.asset_content_type), no X-Content-Type-Options header, and the upload path (lines 127-129) accepts any client content_type with no allowlist; the only diff changes were authorization rewires.
- **lab-tracker-zl9.6**: open
  - No size cap anywhere: dataset_files.py upload (lines 94-98) and visualizations.py upload (lines 132-136) still stream unbounded via store_stream; file_storage.py and note_storage.py are not in the f71ea24..HEAD file list; the config.py diff only hardened auth secret-key validation (INSECURE_AUTH_SECRET_KEYS), no upload-size setting added.
- **lab-tracker-zl9.7**: open
  - src/lab_tracker/app_parts/middleware.py:28-29 still lists /metrics and /readiness in _PUBLIC_PATHS, and observability.py is not in the changed-file list, so the unauthenticated diagnostic payloads are unchanged.
- **lab-tracker-zl9.8**: open
  - src/lab_tracker/app_parts/middleware.py:61 still contains `path.startswith("/_test/")` in _is_public_path, unconditionally exempting the /_test/ prefix from auth in production middleware.
- **lab-tracker-c1u.1**: open
  - safe_attachment_filename (src/lab_tracker/routes/shared.py:119-128) is unchanged and still passes non-ASCII through; dataset_files.py:182-185 and visualizations.py:190-191 still build Content-Disposition via f-string with that value (no RFC 5987 filename* fallback), so CJK filenames still raise UnicodeEncodeError -> 500. The c382cfe changes to routes/dataset_files.py were authorization-only (require_role -> ensure_project_contributor).
- **lab-tracker-c1u.2**: open
  - src/lab_tracker/services/claim_service.py:148-153 — update_claim still uses truthiness fallbacks `supported_by_dataset_ids or claim.supported_by_dataset_ids` / `supported_by_analysis_ids or claim.supported_by_analysis_ids`, so PATCHing either to [] still silently keeps the old links. The diff to this file only swapped require_role for project-scoped authorization.
- **lab-tracker-c1u.3**: open
  - src/lab_tracker/services/analysis_service.py:213-222 — commit_analysis's create_claim call still forwards statement, confidence, status, supported_by_dataset_ids, supported_by_analysis_ids but omits answers_question_ids, which ClaimInput still declares (models.py:553). The analysis_service diff added only authorization and the delete-guard _ensure_analysis_can_be_deleted.
- **lab-tracker-c1u.4**: open
  - src/lab_tracker/services/analysis_service.py:161-172 — the immutability guard still fires only when `analysis.status == AnalysisStatus.COMMITTED`; COMMITTED->ARCHIVED remains an allowed transition (services/shared.py:135), after which environment_hash is freely settable at lines 171-172 via a second PATCH.
- **lab-tracker-c1u.5**: open
  - UTC normalization still skipped: change_set_from_model passes raw row.created_at/updated_at/submitted_at/reviewed_at/committed_at (sqlalchemy_repository_parts/graph_drafts.py:199-209), graph_batches.py:67-68 and 125-126 pass raw timestamps, and the membership _from_row (sqlalchemy_repository_parts/core.py:101-102) passes raw row.created_at/updated_at. The duplicate membership mapper pair still exists in sqlalchemy_mappers.py:124-161; the +27 diff there was external-artifacts plumbing only.
- **lab-tracker-c1u.6**: open
  - src/lab_tracker/sqlalchemy_repository_parts/notes.py:119 still applies the target filter only when `target_entity_type is not None and target_entity_id is not None` (the file's diff was a one-line unrelated change), and routes/notes.py:152-170 still passes both params through with no both-or-neither validation, so half-specified filters are silently ignored.
- **lab-tracker-c1u.7**: partial
  - Write-time validation is now in place: _build_commit_manifest (services/shared.py:467) calls _legacy_external_artifacts (shared.py:374-378), which converts ValueError (including json.JSONDecodeError and pydantic ValidationError, both ValueError subclasses) from external_artifacts_from_metadata into a domain ValidationError, so new commits can no longer store malformed external_artifacts metadata; e9342b9 also made artifacts first-class (models.py:303/313, db_models.py:150). NOT fixed: (a) the read/export path is still unguarded — provenance.py:143 and sqlalchemy_mappers.py:333 call external_artifacts_from_metadata raw, so legacy rows written during the regression window still 500 on export/load; (b) the JSON-LD context (provenance.py:42-80) still lacks the visualization-asset terms contentUrl, fileName, encodingFormat, contentSize, sha256 emitted at provenance.py:304-310, so checksums are still lost under expansion.
- **lab-tracker-c1u.8**: open
  - All six sub-issues persist in src/lab_tracker_client/client.py: _ID_FIELDS still lists analysis_id (index 4) before viz_id (index 6) so LTRecord.id returns the analysis UUID for visualization records (lines 49-59, 88-93); _list_all still pages through the entire collection regardless of limit (lines 746-768, limit is only page size); _request issues httpx calls with no transport-error wrapping (lines 787-808); module import still constructs a client eagerly via `client = client_from_env()` (line 950) which can crash on bad env (float() at line 201); from_env still reads only LAB_TRACKER_BASE_URL with no MCP fallback (line 194); expired-token 401 with no stored credentials still raises the misleading 'LAB_TRACKER_USERNAME and LAB_TRACKER_PASSWORD are required' error (lines 820-825). The faceb89 diff only added evidence-import features.
- **lab-tracker-c1u.9**: open
  - src/lab_tracker/mcp_api_client.py is not in the f71ea24..HEAD file list: get_decision_context still collapses LabTrackerAPIError into a generic 'unavailable' decision error (mcp_api_client.py:352-357), _response_error is unchanged, and every tool in mcp_tools/read.py still constructs a fresh client per call via client_from_env() (read.py:12, 21, 34), performing a new login each time.
- **lab-tracker-aai.2**: open
  - tests/conftest.py untouched in f71ea24..HEAD (no non-admin fixture; git log shows zero commits in range). The only new collaboration test, test_project_contributor_can_use_core_write_routes (tests/test_project_collaboration.py:122-231), asserts POST write authorization (401 for viewer, 201 for contributor) and never exercises list-endpoint scoping; no parametrized scoping tests for /sessions /datasets /analyses /claims /notes /visualizations /goals /batches were added anywhere.
- **lab-tracker-aai.3**: open
  - Grep across tests/ for by-link|promote|close over HTTP finds no TestClient calls to GET /sessions/by-link, POST .../promote, POST .../promote-to-dataset, PATCH .../close, or DELETE /sessions/{id}. New session tests added in range (tests/test_core_api.py:783-870, e.g. test_promote_operational_session_to_dataset, test_closed_operational_session_cannot_be_promoted) call api.promote_* directly at the repository level, not the HTTP routes in routes/sessions.py:83-183.
- **lab-tracker-aai.4**: open
  - The 111 lines added to tests/test_project_collaboration.py in c382cfe are entirely test_project_contributor_can_use_core_write_routes (lines 122-231): contributor/viewer authorization on /questions /sessions /datasets /analyses /claims /visualizations POSTs. No test touches PATCH or DELETE /projects/{id}/members/{user_id}; repo-wide grep for delete_project_membership shows hits only in src (api.py:214, routes/projects.py:240, services/project_service.py:168) and none in tests/, and no last-owner/sole-owner test exists.
- **lab-tracker-aai.6**: open
  - tests/test_smoke.py is unchanged and still contains only `def test_smoke(): assert True` (lines 1-2); no commits in f71ea24..HEAD touched it.
- **lab-tracker-bsk.4**: open
  - The useAuthSession.js change in range (lines 51-60) only differentiates boot-time restore failures: it now keeps the saved token on network errors and clears it only on 401/403 — it adds no refresh scheduling and no mid-session 401 handling. The new useAuthSession.test.jsx (49 lines) tests only boot restore (offline preserve, 401 clear). Grep for refresh/expires_at across frontend_src finds no token-refresh logic and POST /auth/refresh is still never called from the frontend.
- **lab-tracker-bsk.5**: open
  - src/lab_tracker/frontend_src/features/mobile-capture.jsx is not in the f71ea24..HEAD diff and git log shows zero commits in range touching it; uploadCapture remains at line 724 with the same multi-step flow. The shared/upload-queue.js changes in range (retry counting, permanent-rejection statuses, lines 192-245) concern the offline single-file queue drain, not the multi-step capture upload idempotency.
- **lab-tracker-bsk.8**: open
  - git log f71ea24..HEAD shows zero commits touching src/lab_tracker/app_parts/frontend.py or sw.js (frontend_src or bundle); the bare-/app scope gap and rejection-only navigation fallback (sw.js:74) are unchanged.
- **lab-tracker-f6k.2**: partial
  - Partially mitigated: new tests/test_packaging.py:9-27 (test_frontend_package_data_covers_all_bundle_files) plus the pyproject.toml package-data change to "frontend/*" (line 45-47) now fail pytest — which CI runs — if any committed bundle file is excluded from the wheel, covering the 'packaging regressions are invisible' slice. But .github/workflows/ci.yml (lines 1-23) is unchanged: still only ruff + pytest on an editable install — no npm ci/lint/test/build, no frontend_src-vs-bundle drift check, no wheel-install or docker smoke test curling /app/sw.js.
- **lab-tracker-f6k.3**: open
  - No uv.lock or compiled requirements exists (git ls-files shows only package-lock.json, which predates the review range — its history starts at c2a7149); pyproject.toml runtime dependencies (lines 10-21) are still unpinned floors (alembic, fastapi, uvicorn, pydantic-settings with no versions), and ci.yml/Dockerfile still do non-frozen installs. Nothing in f71ea24..HEAD touched lock files.
- **lab-tracker-f6k.4**: open
  - git log f71ea24..HEAD shows zero commits touching Dockerfile or docker-entrypoint.sh; no USER directive added and the 30x migration retry loop is unchanged. (The only container change in range was docker-compose.yml line 15 making LAB_TRACKER_AUTH_SECRET_KEY mandatory, unrelated to this bead.)
- **lab-tracker-c1u.10**: open
  - git log f71ea24..HEAD shows zero commits touching features/sessions/SessionDetailCard.jsx, app-shell.jsx, or hooks/useAnalysisWorkflow.js; the only frontend_src changes in range were useAuthSession.js and shared/upload-queue.js, so all four cited desyncs remain.
- **lab-tracker-c1u.12**: open
  - docs/retained-v1-surface.md is unchanged in f71ea24..HEAD (zero commits); docs/internal-boundaries.md:102 still links to the deleted ../src/lab_tracker/frontend_src/hooks/useProjectWorkspaceActions.js (Glob confirms the file does not exist) — the doc edits in range only updated the external-artifacts section (lines 59-68); README changes in range only added the evidence-inbox feature bullet and `lt import-folder` section, not the quickstart migration step or graph-draft provider corrections.
- **lab-tracker-zl9.3**: open
  - scripts/serve-lan.ps1 is untouched in f71ea24..HEAD: line 101 still runs uvicorn with --host 0.0.0.0 and line 81 only prints an advisory message, with no check of effective auth settings and no override flag. The config.py hardening in range (rejecting placeholder secrets, config.py:59-63) does not help: with no .env, environment defaults to 'local' so is_auth_enabled() returns False (config.py:44-47) and the unauthenticated LAN bind still proceeds.
- **lab-tracker-zl9.5**: open
  - Repo-wide grep for nosniff/X-Content-Type-Options/Content-Security-Policy finds no hits in src/; src/lab_tracker/middleware.py and app_parts/frontend.py are untouched in f71ea24..HEAD, and the api.py changes in range (lines 62-101) only wire project_authorization into services — no header middleware was added.
- **lab-tracker-zl9.9**: open
  - git log f71ea24..HEAD shows zero commits touching src/lab_tracker/routes/auth.py; repo-wide grep for rate limit/lockout finds nothing in src/, and config.py (read in full) gained no setting gating viewer self-registration — the only auth-related change in range was the placeholder-secret validator.

## Per-area assessments

### evidence-inbox

Reviewed commit faceb89 (local folder evidence inbox adapter: lt import-folder, LabTracker.import_evidence_file/upload_note_file/find_evidence_note, build_evidence_metadata, docs/evidence-source-metadata.md). The client/server contract checks out: /notes/upload-file (src/lab_tracker/routes/notes.py:75-114) accepts the metadata JSON form field via parse_metadata_form, the status enum, and merges client evidence_* keys with server source_file_* keys, so the evidence metadata round-trips through GET /notes and re-run idempotency genuinely works; client page size 200 matches the server's pagination cap of 200; metadata keys match the new doc exactly; per-file exception containment means one bad file does not abort the folder; filename-edge cases (non-ASCII, nested paths, sorting) and the --limit/--include/--exclude semantics are sound. Six new findings: (1) the command exits 0 even when every file fails, which blinds the scheduled-automation use case the feature is built for; (2) the provenance hash is computed from a different disk read than the uploaded bytes, a real race in actively-syncing folders that silently corrupts the dedupe/provenance contract; (3) the default dedupe namespace (provider 'local-folder' + root-relative path) collides across different --root folders and the client strips whitespace from external IDs; (4) the known M68 fetch-all defect was propagated into find_evidence_note and amplified to O(files x notes) by the per-file loop; (5) empty files are dry-run-importable but error forever on real runs; (6) uploads buffer whole files in memory. On the other known c1u.8 defects: the CLI correctly builds its own client in main() rather than using the import-time singleton, but three new module-level wrappers (find_evidence_note/upload_note_file/import_evidence_file, client.py:1021-1030) extend the tracked import-time-singleton surface (L19), and raw httpx transport exceptions (M69) still leak from import_evidence_file to library callers — the CLI's broad except Exception happens to contain them. Neither is re-reported since the root defects are tracked and the fixes will cover the new code paths.

### external-artifacts

The external dataset artifacts feature (e9342b9) is largely well built: explicit artifacts are validated at write time with domain ValidationError (fixing the write half of bead c1u.7), the commit-hash payload conditionally includes the new key so legacy commit hashes stay stable, round-trip fidelity between ingestion, storage, and export is faithful (verified empirically, including None metadata values under exclude_none), migration 0025 is a clean single head off 0024 using SQLite batch mode, and all four new JSON-LD context terms used by artifact nodes are defined. However, the c1u.7 supersession is only partial: the unguarded legacy-metadata decode moved from the provenance exporter into dataset_from_model, so one pre-upgrade row with malformed external_artifacts metadata now 500s every dataset read (get/list/query) and makes the row unfixable via the API — empirically reproduced end-to-end. Also found: a create-vs-update commit asymmetry for the documented legacy metadata encoding (reproduced), and two low-severity PROV-O graph coherence gaps (same URI allowed as both Entity and Activity; uri used as @id without IRI validation now reachable as a first-class API field).

### fix-collateral-backend

Reviewed the backend collateral of P1-fix commit c382cfe (f71ea24..HEAD): the project-scoped authorization rollout across question/session/dataset/analysis/claim/visualization services and the dataset_files/visualizations routes, the new delete guards, the SQLite FK/WAL/busy_timeout pragmas in db.py, the repository save() flush additions, and the external-artifact manifest changes in services/shared.py. The authorization wiring itself is sound: ProjectAuthorizationPolicy handles None actors (AuthError), the admin bypass is correctly scoped to Role.ADMIN, auth-disabled mode injects a local admin context, route-level require_role removals in dataset_files.py/visualizations.py are correctly replaced by ensure_project_contributor, and the question-refactor save-order swap plus the parent-row flush() calls are correct consequences of FK enforcement. Storage cleanup on project/dataset/analysis/visualization delete routes is intact, and I empirically confirmed SQLite resolves the projects-cascade vs NO-ACTION primary_question_id interaction within one statement, so project deletion does not regress. The external-artifact manifest merging preserves legacy commit-hash stability. Six new defects found, the most important being a fix-gap: the cq5.2 delete_question guard covers only primary references while the cq5.1 FK pragma now activates the dataset_question_links/claim_questions cascades, so deleting a secondary-linked question silently mutates COMMITTED datasets and invalidates their content-addressed commit_hash (a consequence the original review described but the closed bead did not cover). Related: the FK pragma also newly cascade-deletes graph change sets when their source note is deleted (no guard, no bead), the analysis delete guard over-blocks on terminal/uneditable REJECTED claims with no escape hatch, busy_timeout is set after the WAL conversion pragma, mutation paths now leak entity existence via 404-vs-401 ordering, and the guards are check-then-act on Postgres.

### fix-collateral-frontend

Bundle freshness — the critical check — PASSES: `npm run build` followed by `git diff src/lab_tracker/frontend/` produces zero changes, so the committed app.js is byte-identical to a fresh build of the changed sources; the deceptively small +14-line diff is just the minified bundle occupying ~14 physical lines (numstat 7+/7-), and the new code is provably present (retry_limit, dropReason, lastAttemptAt, 'Could not verify the saved session' all appear in the bundle). The useAuthSession fix is correct on the status-semantics edge cases: throwApiError (shared/api.js:29) always attaches a numeric status for HTTP errors, network TypeErrors leave err.status undefined, fetch never yields status 0, so the strict `err.status === 401 || err.status === 403` check preserves the token offline and clears it only on real rejections — no new defect found there. The rewritten queue's core mechanics are sound (FIFO order preserved via autoIncrement key order, retry loop terminates, network/5xx failures intentionally don't burn the cap, subscriber notify/unsubscribe correct). The real problems are fix-completeness gaps: (1) the bsk.1/H7 transaction-hang fix skipped share-target-inbox.js:54 despite the report naming it, so OS share-sheet migration still hangs after one item per page load; (2) the bsk.2/H8 retryable-401 design cannot ever succeed because drain replays the token frozen at enqueue time, so with the 60-minute TTL the capture is still silently deleted — just after 5 drains instead of 1; (3) bsk.2's required 'surface dropped items to the user' was never implemented (all drain callers discard results.dropped); plus an unserialized drain() that double-posts and double-counts retries under the boot+online+post-migration overlap, and IDB test coverage that uses a hand-rolled add-only fake, leaving remove()/update() and the share-inbox adapter untested — the exact blind spot that let gap (1) through.

### diff-sweep

Cross-cutting security/integrity sweep of f71ea24..HEAD (P1 fix commit c382cfe plus the two unreviewed features faceb89 and e9342b9). The authz migration from global WRITE_ROLES to per-project require_contributor is sound and consistently applied across services and the dataset-files/visualizations upload routes (it tightens, not loosens, access: global EDITORs without project membership lose write access; project contributors gain it as intended); the config placeholder-secret rejection, docker-compose mandatory secret, SQLite FK/WAL pragmas, useAuthSession 401/403-only token clearing, and refactor_question save-order fix all match their bead prescriptions and introduce no injection, secret, or new-config-default issues. The external-artifact commit-hash design carefully preserves legacy hashes (artifacts only enter the hashed payload when they differ from the legacy metadata encoding). Remaining problems are concentrated in fix gaps and the new import-folder adapter: (1) the upload-queue H8 fix still silently deletes offline captures because retries reuse the stale stored token and no caller reads results.dropped; (2) the delete_question guard ignores ondelete=CASCADE question links on committed datasets and claim answer links — cascades the new FK pragma just activated on SQLite — allowing silent mutation of committed manifests under a stale commit_hash; (3) dataset_from_model's new legacy-metadata fallback turns malformed pre-0025 external_artifacts metadata into 500s on all dataset reads (wider blast radius than tracked M33) with no migration repair; plus an O(files x notes) dedupe pattern, a symlink-following path-handling gap and a hash/upload TOCTOU in the import CLI, and a broken README quick-start for the new command. No new findings in .beads, docs/runs, or areas already covered by tracked open beads were re-reported.

## Completeness critic

The re-review covered the evidence-inbox client (cli.py/client.py: 5 findings), the upload-queue/share-inbox page code (4 findings), the FK-pragma cascade consequences in delete guards (question/note/analysis/db_models), config secret denylist, and dataset commit-guard/mapper edges of the external-artifacts feature. However, three large changed areas in f71ea24..HEAD produced zero findings and were evidently not examined: (1) commit c382cfe's project-scoped authorization overhaul — api.py now injects a ProjectAuthorizationPolicy into six services, claim_service.py/session_service.py/visualization_service.py replaced global require_role(WRITE_ROLES) with per-project require_contributor (in update/delete paths the entity is fetched BEFORE the auth check), and routes/dataset_files.py + routes/visualizations.py downgraded-then-upgraded from require_role+ensure_project_read to ensure_project_contributor; cq5.2 is only PARTIAL, and no authz finding exists at all; (2) the core manifest logic of the external-artifacts feature: services/shared.py gained ~84 lines of merge/normalize/payload logic that feeds _compute_commit_hash, including a conditional that omits external_artifacts from the hashed payload when they equal legacy-metadata-derived artifacts, an artifact identity key that excludes content_hash, and a _legacy_external_artifacts call that can raise inside hash computation; provenance_ingestion.py (75 changed lines), models.py (62 lines), and migration 0025 were likewise unmentioned; (3) the service-worker writer side of the share pipeline: src/lab_tracker/frontend/sw.js is hand-maintained (scripts/build-frontend.mjs only bundles app.jsx into app.js), did not change in the diff, and writes share records into the same IndexedDB the fixed page-side share-target-inbox.js reads — schema/shape consistency was never checked, nor was it verified that the rebuilt committed bundle app.js actually embeds the fixed upload-queue code; the 4-line useAuthSession.js fix (fee verdicts marked FIXED) hinges on err.status being populated, which deserves a second opinion given network errors carry no status.
